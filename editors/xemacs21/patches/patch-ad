--- lib-src/ootags.c.orig	Mon Aug  7 22:18:37 2000
+++ lib-src/ootags.c	Mon Aug  7 22:19:31 2000
@@ -123,7 +123,7 @@
 #endif /* LONG_OPTIONS */
 
 #ifdef ETAGS_REGEXPS
-# include <regex.h>
+# include "../src/regex.h"
 #endif /* ETAGS_REGEXPS */
 
 /* Define CTAGS to make the program "ctags" compatible with the usual one.
