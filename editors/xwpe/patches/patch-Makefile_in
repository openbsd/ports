Index: Makefile.in
--- Makefile.in.orig
+++ Makefile.in
@@ -882,7 +882,7 @@ AM_CFLAGS = $(XWPE_STDFLAG) $(NCURSESW_CFLAGS) $(X_CFL
 	$(JSONC_CFLAGS) -DLIBRARY_DIR=\"$(pkglibdir)\" \
 	-DXWPE_INFODIR=\"$(infodir)\" -Wno-incompatible-pointer-types \
 	-Wno-implicit-function-declaration -Wno-int-conversion \
-	-Wno-old-style-declaration $(am__append_2)
+	$(am__append_2)
 
 # Force-include config.h without modifying every .c file
 AM_CPPFLAGS = -include config.h
