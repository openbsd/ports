drop call to an ncurses function we don't have

Index: src/pspg.c
--- src/pspg.c.orig
+++ src/pspg.c
@@ -6726,12 +6726,6 @@ refresh:
 
 #endif
 
-#if NCURSES_EXT_FUNCS
-
-	reset_color_pairs();
-
-#endif
-
 	/* try to reinit original color palette */
 	start_color();
 
