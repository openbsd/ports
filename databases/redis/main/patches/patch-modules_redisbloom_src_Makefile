Index: modules/redisbloom/src/Makefile
--- modules/redisbloom/src/Makefile.orig
+++ modules/redisbloom/src/Makefile
@@ -29,9 +29,11 @@ else
 include $(ROOT)/deps/readies/mk/main
 
 # RedisBloom only supports 64-bit architectures
-ifneq ($(ARCH),x64)
-ifneq ($(ARCH),arm64v8)
-$(error RedisBloom only supports 64-bit architectures (x64, arm64v8). Current architecture: $(ARCH))
+ifneq ($(ARCH),amd64)
+ifneq ($(ARCH),arm64)
+ifneq ($(ARCH),sparc64)
+$(error RedisBloom only supports 64-bit architectures (amd64, arm64, sparc64). Current architecture: $(ARCH))
+endif
 endif
 endif
 
