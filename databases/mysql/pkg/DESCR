MySQL is a multi-threaded SQL database with a command syntax very similar
to mSQL.

This is the client portion of MySQL, it provides the client binaries,
mysqlclient library and headers for the MySQL client API.
