libnice is a library that implements the Interactive Connectivity Establishment
(ICE) standard (RFC 5245 & RFC 8445)!

It provides a GLib-based library, libnice, as well as GStreamer elements to use
it.
