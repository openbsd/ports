$OpenBSD: patch-node_sbin_munin-node,v 1.3 2013/01/18 12:23:24 gonzalo Exp $
--- node/sbin/munin-node.orig	Wed Jan  9 19:46:54 2013
+++ node/sbin/munin-node	Fri Jan 11 01:08:03 2013
@@ -53,6 +53,9 @@ sub main
         $ENV{$key} =~ /^(.*)$/;
         $ENV{$key} = $1;
     }
+    # plugins run in taint mode because the uid is changed, so the path
+    # must not contain writable directories.
+    $ENV{PATH}='/bin:/sbin:/usr/bin:/usr/sbin:${PREFIX}/bin:${PREFIX}/sbin';
 
     $0 =~ /([^\/]*)$/;
     $0 = $1;
