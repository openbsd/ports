Backport fix from upstream
291bfe912c600b0c98a72c7d2684de8f8249bc76

$OpenBSD: patch-master_lib_Munin_Master_Node_pm,v 1.1 2021/11/23 08:28:29 kirby Exp $

Index: master/lib/Munin/Master/Node.pm
--- master/lib/Munin/Master/Node.pm.orig
+++ master/lib/Munin/Master/Node.pm
@@ -85,7 +85,7 @@ sub _do_connect {
         $self->{reader} = $self->{writer} = IO::Socket::INET6->new(
 		PeerAddr  => $uri->host,
 		PeerPort  => $self->{port} || 4949,
-		LocalAddr => _get_node_or_global_setting("local_address"),
+		LocalAddr => $self->_get_node_or_global_setting("local_address"),
 		Proto     => 'tcp', 
 		MultiHomed => 1,
 		Timeout   => $config->{timeout}
@@ -239,12 +239,12 @@ sub list_plugins {
     my ($self) = @_;
 
     # Check for one on this node- if not, use the global one
-    my $use_node_name = _get_node_or_global_setting("use_node_name");
+    my $use_node_name = $self->_get_node_or_global_setting("use_node_name");
     my $host = $use_node_name
         ? $self->{node_name}
         : $self->{host};
 
-    my $use_default_node = _get_node_or_global_setting("use_default_node");
+    my $use_default_node = $self->_get_node_or_global_setting("use_default_node");
 
     if (! $use_default_node && ! $host) {
 	die "[ERROR] Couldn't find out which host to list on $host.\n";
