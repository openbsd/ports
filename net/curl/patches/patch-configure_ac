$OpenBSD: patch-configure_ac,v 1.1 2021/07/21 19:17:02 naddy Exp $

pkg-config --libs-only-l outputs leading whitespace.

Index: configure.ac
--- configure.ac.orig
+++ configure.ac
@@ -2542,6 +2542,7 @@ if test X"$want_h2" != Xno; then
     CPPFLAGS="$CPPFLAGS $CPP_H2"
     LIBS="$LIB_H2 $LIBS"
     LIB_H2_NAME=${LIB_H2#"-l"}
+    LIB_H2_NAME=`echo $LIB_H2 | $SED -e 's/-l//'`
 
     # use nghttp2_session_set_local_window_size to require nghttp2
     # >= 1.12.0
