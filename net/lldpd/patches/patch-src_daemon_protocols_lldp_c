$OpenBSD: patch-src_daemon_protocols_lldp_c,v 1.3 2021/08/29 12:41:09 sthen Exp $

Silence noisy logs seen sometimes if interface is admin-up but hw-down
"unable to send packet on real device for ix1: No buffer space available"

Index: src/daemon/protocols/lldp.c
--- src/daemon/protocols/lldp.c.orig
+++ src/daemon/protocols/lldp.c
@@ -505,8 +505,9 @@ end:
 
 	if (interfaces_send_helper(global, hardware,
 		(char *)packet, pos - packet) == -1) {
-		log_warn("lldp", "unable to send packet on real device for %s",
-		    hardware->h_ifname);
+		if (errno != ENOBUFS)
+			log_warn("lldp", "unable to send packet on real device for %s",
+			    hardware->h_ifname);
 		free(packet);
 		return ENETDOWN;
 	}
