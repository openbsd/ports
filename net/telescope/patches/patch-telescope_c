Index: telescope.c
--- telescope.c.orig
+++ telescope.c
@@ -453,8 +453,7 @@ handle_dispatch_imsg(int fd, int event, void *data)
 	struct download	*d;
 	const char	*h;
 	char		*str, *page;
-	ssize_t		 n;
-	int		 code;
+	int		 code, n;
 
 	if (event & EV_READ) {
 		if (imsgbuf_read(imsgbuf) == -1)
@@ -466,8 +465,8 @@ handle_dispatch_imsg(int fd, int event, void *data)
 	}
 
 	for (;;) {
-		if ((n = imsg_get(imsgbuf, &imsg)) == -1)
-			err(1, "imsg_get");
+		if ((n = imsgbuf_get(imsgbuf, &imsg)) == -1)
+			err(1, "imsgbuf_get");
 		if (n == 0)
 			break;
 
