Index: net.c
--- net.c.orig
+++ net.c
@@ -576,8 +576,7 @@ handle_dispatch_imsg(int fd, int event, void *d)
 	struct imsg	 imsg;
 	struct req	*req;
 	struct get_req	 r;
-	ssize_t		 n;
-	int		 certok;
+	int		 certok, n;
 
 	if (event & EV_READ) {
 		if (imsgbuf_read(ibuf) == -1) {
@@ -593,8 +592,8 @@ handle_dispatch_imsg(int fd, int event, void *d)
 	}
 
 	for (;;) {
-		if ((n = imsg_get(ibuf, &imsg)) == -1)
-			err(1, "imsg_get");
+		if ((n = imsgbuf_get(ibuf, &imsg)) == -1)
+			err(1, "imsgbuf_get");
 		if (n == 0)
 			break;
 		switch (imsg_get_type(&imsg)) {
