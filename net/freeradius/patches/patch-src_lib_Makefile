$OpenBSD: patch-src_lib_Makefile,v 1.4 2008/01/21 22:21:24 rui Exp $
--- src/lib/Makefile.orig	Sat Nov 24 08:29:17 2007
+++ src/lib/Makefile	Mon Jan 21 16:21:31 2008
@@ -39,7 +39,7 @@ TARGET		= $(LIBPREFIX)freeradius-radius
 all: $(TARGET).la
 
 $(TARGET).la: $(LT_OBJS)
-	$(LIBTOOL) --mode=link $(CC) -release $(RADIUSD_VERSION) \
+	$(LIBTOOL) --mode=link $(CC) \
 	$(LDFLAGS) $(LINK_MODE) -o $@ -rpath $(libdir) $^
 
 $(LT_OBJS): $(INCLUDES)
@@ -52,5 +52,3 @@ install: all
 	$(INSTALL) -d -m 755	$(R)$(libdir)
 	$(LIBTOOL) --mode=install $(INSTALL) -c $(TARGET).la \
 		$(R)$(libdir)/$(TARGET).la
-	rm -f $(R)$(libdir)/$(TARGET)-$(RADIUSD_VERSION).la;
-	ln -s $(TARGET).la $(R)$(libdir)/$(TARGET)-$(RADIUSD_VERSION).la
