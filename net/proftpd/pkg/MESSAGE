+------------
| Sample configurations have been installed already.  You can
| find them in ${PREFIX}/share/examples/proftpd.  To get started 
| quickly, you can copy one to /etc/proftpd.conf and edit it 
| appropriately.
|
| See ${HOMEPAGE} for more detailed configuration information.
|
| If you will be running proftpd from inetd, use a line like:
|
| ftp stream tcp nowait root /usr/local/sbin/proftpd proftpd
+------------
