$OpenBSD: patch-src_iosource_Packet_cc,v 1.3 2019/11/25 07:57:43 ajacoutot Exp $

Index: src/iosource/Packet.cc
--- src/iosource/Packet.cc.orig
+++ src/iosource/Packet.cc
@@ -92,6 +92,7 @@ int Packet::GetLinkHeaderSize(int link_type)
 	case DLT_IEEE802_11_RADIO:	// 802.11 plus RadioTap
 		return 59;
 
+#ifndef __OpenBSD__
 	case DLT_NFLOG:
 		// Linux netlink NETLINK NFLOG socket log messages
 		// The actual header size is variable, but we return the minimum
@@ -99,6 +100,7 @@ int Packet::GetLinkHeaderSize(int link_type)
 		// least 2 bytes each for the type and length values assoicated with
 		// the final TLV carrying the packet payload.
 		return 8;
+#endif
 
 	case DLT_RAW:
 		return 0;
@@ -403,6 +405,7 @@ void Packet::ProcessLayer2()
 		break;
 		}
 
+#ifndef __OpenBSD__
 	case DLT_NFLOG:
 		{
 		// See https://www.tcpdump.org/linktypes/LINKTYPE_NFLOG.html
@@ -481,6 +484,7 @@ void Packet::ProcessLayer2()
 
 		break;
 		}
+#endif
 
 	default:
 		{
