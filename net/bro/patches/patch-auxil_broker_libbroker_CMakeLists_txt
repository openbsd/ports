Index: auxil/broker/libbroker/CMakeLists.txt
--- auxil/broker/libbroker/CMakeLists.txt.orig
+++ auxil/broker/libbroker/CMakeLists.txt
@@ -93,8 +93,8 @@ if (ENABLE_SHARED)
     add_library(broker SHARED ${BROKER_SRC})
     set_target_properties(
         broker
-        PROPERTIES SOVERSION ${BROKER_SOVERSION}
-                   VERSION ${BROKER_VERSION_MAJOR}.${BROKER_VERSION_MINOR}
+        PROPERTIES SOVERSION ${LIBbroker_VERSION}
+                   VERSION ${LIBbroker_VERSION}
                    MACOSX_RPATH true
                    OUTPUT_NAME broker)
     target_link_libraries(broker PUBLIC ${LINK_LIBS})
