#!/bin/ksh

daemon="${TRUEPREFIX}/bin/telemt"
daemon_flags="${SYSCONFDIR}/telemt/config.toml"
daemon_logger="daemon.info"
daemon_user="_telemt"

. /etc/rc.d/rc.subr

rc_bg=YES
rc_reload=NO

rc_cmd $1
