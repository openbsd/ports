https://sources.debian.org/patches/pavuk/0.9.35-6.1/12-fix_FORTIFY_SOURCES.patch/

Index: src/tools.c
--- src/tools.c.orig
+++ src/tools.c
@@ -1726,7 +1726,7 @@ int tl_save_text_file(char *filename, char *content, i
   if(length < 0)
     length = strlen(content);
 
-  if((fd = open(filename, O_WRONLY | O_BINARY | O_CREAT | O_TRUNC), 0644) < 0)
+  if((fd = open(filename, O_WRONLY | O_BINARY | O_CREAT | O_TRUNC, 0644)) < 0)
   {
     xperror(filename);
     return -1;
