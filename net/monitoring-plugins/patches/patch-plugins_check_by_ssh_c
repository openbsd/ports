Index: plugins/check_by_ssh.c
--- plugins/check_by_ssh.c.orig
+++ plugins/check_by_ssh.c
@@ -74,6 +74,7 @@ main (int argc, char **argv)
 
 	remotecmd = "";
 	comm_append(SSH_COMMAND);
+	comm_append("-o WarnWeakCrypto=no");
 
 	setlocale (LC_ALL, "");
 	bindtextdomain (PACKAGE, LOCALEDIR);
