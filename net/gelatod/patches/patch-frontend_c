https://codeberg.org/fobser/gelatod/pulls/1

Index: frontend.c
--- frontend.c.orig
+++ frontend.c
@@ -364,8 +364,7 @@ frontend_dispatch_main(int fd, short event, void *bula
 	struct imsg		 imsg;
 	struct imsgev		*iev = bula;
 	struct imsgbuf		*ibuf = &iev->ibuf;
-	ssize_t			 n;
-	int			 shut = 0;
+	int			 n, shut = 0;
 
 	if (event & EV_READ) {
 		if ((n = imsgbuf_read(ibuf)) == -1)
@@ -383,8 +382,8 @@ frontend_dispatch_main(int fd, short event, void *bula
 	}
 
 	for (;;) {
-		if ((n = imsg_get(ibuf, &imsg)) == -1)
-			fatal("%s: imsg_get error", __func__);
+		if ((n = imsgbuf_get(ibuf, &imsg)) == -1)
+			fatal("%s: imsgbuf_get error", __func__);
 		if (n == 0)	/* No more messages. */
 			break;
 
