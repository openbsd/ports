Index: src/chkshsgr.c
--- src/chkshsgr.c.orig
+++ src/chkshsgr.c
@@ -1,12 +1,13 @@
 /* Public domain. */
 
 #include <unistd.h>
+#include <strings.h>
 
 int main()
 {
-  short x[4];
+  gid_t x;
 
-  x[0] = x[1] = 0;
-  if (getgroups(1,x) == 0) if (setgroups(1,x) == -1) _exit(1);
+  bzero(&x, sizeof x);
+  if (getgroups(1,&x) == 0) if (setgroups(1,&x) == -1) _exit(1);
   _exit(0);
 }
