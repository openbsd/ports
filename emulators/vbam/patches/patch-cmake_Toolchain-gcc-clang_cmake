Index: cmake/Toolchain-gcc-clang.cmake
--- cmake/Toolchain-gcc-clang.cmake.orig
+++ cmake/Toolchain-gcc-clang.cmake
@@ -56,7 +56,6 @@ endif()
 if(CMAKE_BUILD_TYPE STREQUAL "Debug")
     add_compile_options(-ggdb3 -fno-omit-frame-pointer -Wall -Wextra)
 else()
-    add_compile_options(-O3 -ffast-math -fomit-frame-pointer)
 endif()
 
 # for some reason this is necessary
