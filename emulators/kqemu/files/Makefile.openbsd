# $OpenBSD

NOMAN = noman
CFLAGS += -Wall
COMMON = kqemu-mod-${MACHINE_ARCH:S/amd64/x86_64/}.o
SRCS = kqemu-openbsd.c
OBJS = ${COMMON}
LKM = kqemu
COMBINED = kqemu.o

.include <bsd.lkm.mk>

${COMMON}:
	gmake -C common all

clean:
	rm -f $(COMMON)
	gmake -C common clean
