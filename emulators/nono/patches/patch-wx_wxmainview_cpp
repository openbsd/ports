$OpenBSD: patch-wx_wxmainview_cpp,v 1.1.1.1 2020/07/06 17:42:25 gonzalo Exp $

Index: wx/wxmainview.cpp
--- wx/wxmainview.cpp.orig
+++ wx/wxmainview.cpp
@@ -41,7 +41,8 @@
 // ちょっと無駄なだけなので (動かないことに比べれば) 気にしない。
 //
 // 今のところ NetBSD 5.0 以上(と Linux (CentOS 6.3/x86_64)) が該当する。
-#if defined(__NetBSD__) && __NetBSD_Version__ >= 500000000
+#if defined(__NetBSD__) && __NetBSD_Version__ >= 500000000 \
+	|| defined(__OpenBSD__)
 #define DONT_USE_REFRESH 1
 #endif
 
