Index: CMakeLists.txt
--- CMakeLists.txt.orig
+++ CMakeLists.txt
@@ -204,9 +204,9 @@ else()
     endif()
     set(CMAKE_CXX_FLAGS_DEBUG "${WARN_FLAGS} -fPIC -ggdb -pipe")
     if (USE_OPENMP)
-        set(CMAKE_CXX_FLAGS_RELEASE "${WARN_FLAGS} -fPIC -O3 -g -pipe -fopenmp")
+        set(CMAKE_CXX_FLAGS_RELEASE "${WARN_FLAGS} -fPIC -pipe -fopenmp")
     else()
-        set(CMAKE_CXX_FLAGS_RELEASE "${WARN_FLAGS} -fPIC -O3 -g -pipe")
+        set(CMAKE_CXX_FLAGS_RELEASE "${WARN_FLAGS} -fPIC -pipe")
     endif()
 endif()
 
