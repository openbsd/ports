COMMENT =		python language-detection library

MODPY_EGG_VERSION =	1.0.9
DISTNAME =		langdetect-${MODPY_EGG_VERSION}
PKGNAME=		py-${DISTNAME}
CATEGORIES =		textproc
HOMEPAGE =		https://github.com/Mimino666/langdetect

MAINTAINER =		Kirill A. Korinsky <kirill@korins.ky>

# Apache-2.0
PERMIT_PACKAGE =	Yes

FLAVORS =		python3
FLAVOR =		python3

MODULES =		lang/python
MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools

RUN_DEPENDS =		devel/py-six${MODPY_FLAVOR}

.include <bsd.port.mk>
