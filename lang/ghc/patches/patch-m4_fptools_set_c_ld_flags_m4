Index: m4/fptools_set_c_ld_flags.m4
--- m4/fptools_set_c_ld_flags.m4.orig
+++ m4/fptools_set_c_ld_flags.m4
@@ -95,6 +95,11 @@ AC_DEFUN([FPTOOLS_SET_C_LD_FLAGS],
         $4="$$4 -z wxneeded"
         ;;
 
+    i386-*-openbsd*)
+        # On i386, we also need -z notext in addition to -z wxneeded.
+        $3="$$3 -Wl,-z,wxneeded -Wl,-z,notext"
+        $4="$$4 -z wxneeded -z notext"
+        ;;
     esac
 
     AC_MSG_RESULT([done])
