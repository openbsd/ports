$OpenBSD: patch-libgfortran_intrinsics_eoshift2_c,v 1.1 2014/08/18 07:42:12 pascal Exp $
--- libgfortran/intrinsics/eoshift2.c.orig	Thu Jan  2 23:25:45 2014
+++ libgfortran/intrinsics/eoshift2.c	Thu Aug 14 16:54:00 2014
@@ -78,8 +78,8 @@ eoshift2 (gfc_array_char *ret, const gfc_array_char *a
       ret->offset = 0;
       ret->dtype = array->dtype;
 
-      /* xmalloc allocates a single byte for zero size.  */
-      ret->base_addr = xmalloc (size * arraysize);
+      /* xmallocarray allocates a single byte for zero size.  */
+      ret->base_addr = xmallocarray (arraysize, size);
 
       for (i = 0; i < GFC_DESCRIPTOR_RANK (array); i++)
         {
