$OpenBSD: patch-libgfortran_generated_all_l16_c,v 1.1 2014/08/18 07:42:11 pascal Exp $
--- libgfortran/generated/all_l16.c.orig	Thu Jan  2 23:25:45 2014
+++ libgfortran/generated/all_l16.c	Thu Aug 14 16:54:01 2014
@@ -101,8 +101,7 @@ all_l16 (gfc_array_l16 * const restrict retarray, 
       retarray->offset = 0;
       retarray->dtype = (array->dtype & ~GFC_DTYPE_RANK_MASK) | rank;
 
-      alloc_size = sizeof (GFC_LOGICAL_16) * GFC_DESCRIPTOR_STRIDE(retarray,rank-1)
-    		   * extent[rank-1];
+      alloc_size = GFC_DESCRIPTOR_STRIDE(retarray,rank-1) * extent[rank-1];
 
       if (alloc_size == 0)
 	{
@@ -111,7 +110,7 @@ all_l16 (gfc_array_l16 * const restrict retarray, 
 	  return;
 	}
       else
-	retarray->base_addr = xmalloc (alloc_size);
+	retarray->base_addr = xmallocarray (alloc_size, sizeof (GFC_LOGICAL_16));
     }
   else
     {
