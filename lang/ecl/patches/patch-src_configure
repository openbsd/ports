Index: src/configure
--- src/configure.orig
+++ src/configure
@@ -6234,7 +6234,7 @@ LSP_FEATURES="${LSP_FEATURES} :bsd"
                 BUNDLE_LDFLAGS="-shared ${LDFLAGS}"
                 ECL_LDRPATH="-Wl,--rpath,~A"
                 clibs="-lpthread ${clibs}"
-                SONAME="${SHAREDPREFIX}ecl.${SHAREDEXT}.SOVERSION"
+                SONAME="${SHAREDPREFIX}ecl.${SHAREDEXT}.${ECLLIB_VERSION}"
                 SONAME_LDFLAGS="-Wl,-soname,SONAME"
 
 LSP_FEATURES="${LSP_FEATURES} :unix"
@@ -11644,7 +11644,7 @@ if test ${enable_shared} = "yes"; then
 
 printf "%s\n" "#define ENABLE_DLOPEN 1" >>confdefs.h
 
-  LSP_LIBRARIES="${SHAREDPREFIX}ecl.${SHAREDEXT}"
+  LSP_LIBRARIES="${SHAREDPREFIX}ecl.${SHAREDEXT}.${ECLLIB_VERSION}"
 
 
 COMPILATION_FEATURES=":wants-dlopen ${COMPILATION_FEATURES}"
