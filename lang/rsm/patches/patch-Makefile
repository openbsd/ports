--- Makefile.orig.port	Mon Dec  9 08:01:21 2024
+++ Makefile	Sat Feb 21 09:38:45 2026
@@ -28,7 +28,7 @@
 # SPDX-FileCopyrightText:  © 2020 David Wicksell <dlw@linux.com>
 # SPDX-License-Identifier: AGPL-3.0-or-later
 
-CC       := gcc
+CC       ?= gcc
 CFLAGS   += -std=c99 -Wall -Wextra -Wmissing-prototypes -pedantic -fsigned-char -fwrapv
 CPPFLAGS := -Iinclude
 LDLIBS   := -lcrypt -lm
@@ -44,11 +44,11 @@
 OS       != uname
 GIT_SHA  != git rev-parse --short=10 HEAD 2>/dev/null; true
 
-.ifdef prefix
-    PREFIX := $(prefix)
-.else
-    PREFIX := /usr/local
-.endif
+#.ifdef prefix
+#    PREFIX := $(prefix)
+#.else
+#    PREFIX := /usr/local
+#.endif
 
 .if ($(OS) == OpenBSD)
     LDLIBS := -lm
@@ -68,7 +68,6 @@
 .       endif
 .   endif
 .else
-    CFLAGS   += -O3
     CPPFLAGS += -DNDEBUG
 .endif
 
