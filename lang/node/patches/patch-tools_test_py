$OpenBSD: patch-tools_test_py,v 1.3 2017/08/06 15:01:45 abieber Exp $
Index: tools/test.py
--- tools/test.py.orig
+++ tools/test.py
@@ -858,9 +858,9 @@ class Context(object):
 
   def GetVm(self, arch, mode):
     if arch == 'none':
-      name = 'out/Debug/node' if mode == 'debug' else 'out/Release/node'
+      name = '${WRKDIST}/out/Debug/node' if mode == 'debug' else '${WRKDIST}/out/Release/node'
     else:
-      name = 'out/%s.%s/node' % (arch, mode)
+      name = '${WRKDIST}/out/%s.%s/node' % (arch, mode)
 
     # Currently GYP does not support output_dir for MSVS.
     # http://code.google.com/p/gyp/issues/detail?id=40
