Index: CMakeLists.txt
--- CMakeLists.txt.orig
+++ CMakeLists.txt
@@ -67,10 +67,6 @@ execute_process(COMMAND ${CMAKE_COMMAND}
 if (GIT_VERSION)
     message(STATUS "${PROJECT_NAME} build version is: ${GIT_VERSION}")
     add_definitions(-DGIT_VERSION)
-else()
-    message(WARNING "Git binary not found. Build version will be set to NULL. Install Git package
-    or use -DGIT_BINARY to set path to git binary.")
-    set(VERSION "NULL")
 endif ()
 
 #OS Check Placeholders. Will be used for BINDIST
@@ -84,6 +80,10 @@ if ("${CMAKE_SYSTEM_NAME}" MATCHES "FreeBSD"
     set(_OS_BSD_ 1)
 endif ()
 
+if ("${CMAKE_SYSTEM_NAME}" MATCHES "OpenBSD")
+    set(_OS_OPENBSD_ 1)
+endif ()
+
 if ("${CMAKE_SYSTEM_NAME}" MATCHES "Darwin")
     set(_OS_DARWIN_ 1)
     add_definitions(-DMACOSX)
@@ -177,7 +177,7 @@ else ()
     add_definitions(-DRELEASE)
     # Use CMAKE_INSTALL_PREFIX as storage of data,gfx, etc.. Useful only on *nix OS.
     if(USE_PREFIX_DATA_DIR)
-        if ("${CMAKE_SYSTEM_NAME}" MATCHES "(Linux|FreeBSD|Darwin)")
+        if ("${CMAKE_SYSTEM_NAME}" MATCHES "(Linux|FreeBSD|Darwin|OpenBSD)")
             set(CMAKE_INSTALL_DATAROOTDIR "${CMAKE_INSTALL_PREFIX}/share/cataclysm-dda")
             add_definitions(-DPREFIX=${CMAKE_INSTALL_PREFIX})
             configure_file(
@@ -217,7 +217,7 @@ message(STATUS "See doc/c++/COMPILING-CMAKE.md for det
 
 if (NOT MSVC)
     set(CATA_WARNINGS
-            "-Werror -Wall -Wextra \
+            "-Wall -Wextra \
              -Wformat-signedness \
              -Wlogical-op \
              -Wmissing-declarations \
@@ -444,7 +444,7 @@ if (CATA_CLANG_TIDY_PLUGIN OR CATA_CLANG_TIDY_EXECUTAB
     add_subdirectory(tools/clang-tidy-plugin)
 endif()
 
-install(DIRECTORY doc TYPE DOC)
+#install(DIRECTORY doc TYPE DOC)
 
 if (RELEASE)
     install(FILES
