COMMENT =		deal pysol cards

MODPY_DISTV =	0.18.0
DISTNAME =		pysol_cards-${MODPY_DISTV}
PKGNAME =		pysol-cards-${MODPY_DISTV}
REVISION =		0

CATEGORIES =		games

# BSD
PERMIT_PACKAGE =	Yes

MODULES =		lang/python

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools

TEST_DEPENDS =		devel/py-test-cov

.include <bsd.port.mk>
