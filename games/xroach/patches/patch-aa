--- Makefile.orig	Tue Jun  8 20:00:00 1993
+++ Makefile	Fri Mar 31 15:51:26 2000
@@ -1,9 +1,17 @@
-#	@(#)Makefile	8.1 (Berkeley) 6/8/93
+#	@(#)Makefile	8.1 (Berkeley) 5/31/93
 
 PROG=	xroach
-MAN6=	xroach.0
-CFLAGS+= -I${.CURDIR}/bitmaps
-DPADD=	${LIBX} ${LIBM}
-LDADD=	-lX11/X11 -lm
+MAN=	xroach.6
+
+DPADD+=		${X11BASE}/lib/libX11.a ${LIBM}
+CFLAGS+=	-I${X11BASE}/include -I${.CURDIR}/bitmaps
+LDDESTDIR+=	-L${X11BASE}/lib
+LDADD+=		${LDDESTDIR}	-lX11 -lm
+
+MANDIR=	${PREFIX}/man/cat
+
+BINOWN=	root
+BINDIR=	${PREFIX}/bin
+BINMODE=755
 
 .include <bsd.prog.mk>
