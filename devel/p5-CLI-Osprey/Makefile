COMMENT =	MooX::Options + MooX::Cmd + Sanity

DISTNAME =	CLI-Osprey-0.09

CATEGORIES =	devel

# Perl
PERMIT_PACKAGE =	Yes

CPAN_AUTHOR =		ARODLAND

MODULES =		cpan

RUN_DEPENDS =		devel/p5-Getopt-Long-Descriptive>=0.10 \
			devel/p5-Module-Runtime \
			devel/p5-Moo \
			devel/p5-Path-Tiny
TEST_DEPENDS =		devel/p5-Test-Lib \
			sysutils/p5-Capture-Tiny

CONFIGURE_STYLE =	modbuild tiny

PKG_ARCH =		*

.include <bsd.port.mk>
