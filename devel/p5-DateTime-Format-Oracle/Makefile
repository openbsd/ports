COMMENT =	parse and format Oracle dates and timestamps

DISTNAME =	DateTime-Format-Oracle-0.06

CATEGORIES =	devel

# Perl
PERMIT_PACKAGE =	Yes

MODULES =		cpan

RUN_DEPENDS =           converters/p5-Convert-NLS_DATE_FORMAT>=0.03 \
			devel/p5-DateTime \
			devel/p5-DateTime-Format-Builder

.include <bsd.port.mk>
