$OpenBSD: patch-pubkey_h,v 1.3 2020/11/17 20:51:34 sthen Exp $

Index: pubkey.h
--- pubkey.h.orig
+++ pubkey.h
@@ -36,7 +36,7 @@
 
 #include "config.h"
 
-#if CRYPTOPP_MSC_VERSION
+#if defined(CRYPTOPP_MSC_VERSION)
 # pragma warning(push)
 # pragma warning(disable: 4702)
 #endif
@@ -2248,7 +2248,7 @@ class DL_ES : public KEYS (public)
 
 NAMESPACE_END
 
-#if CRYPTOPP_MSC_VERSION
+#if defined(CRYPTOPP_MSC_VERSION)
 # pragma warning(pop)
 #endif
 
