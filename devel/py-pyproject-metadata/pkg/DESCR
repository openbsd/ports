pyproject-metadata provides a dataclass for PEP 621 metadata with
support for core metadata generation.

This project does not parse pyproject.toml containing PEP 621 metadata
itself, but given a Python data structure representing already-parsed
metadata, it will validate this input and generate a PEP 643-compliant
metadata file (e.g. PKG-INFO).
