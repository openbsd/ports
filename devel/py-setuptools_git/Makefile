COMMENT=		setuptools revision control system plugin for Git

MODPY_DISTV=	1.2
DISTNAME=		setuptools-git-${MODPY_DISTV}
PKGNAME=		py-${DISTNAME}
REVISION=		9

CATEGORIES=		devel

# BSD
PERMIT_PACKAGE=	Yes

MODULES=		lang/python

MODPY_PI=		Yes
MODPY_PYBUILD=		setuptools

RUN_DEPENDS=		devel/py-setuptools

.include <bsd.port.mk>
