COMMENT =		utility functions dealing with signals

MODULES =		cpan
PKG_ARCH =		*
DISTNAME =		Signal-Mask-0.009
CATEGORIES =		devel

# Perl
PERMIT_PACKAGE =	Yes

CPAN_AUTHOR =		LEONT

BUILD_DEPENDS =		${RUN_DEPENDS}

RUN_DEPENDS =		devel/p5-IPC-Signal

.include <bsd.port.mk>
