COMMENT =	on the fly conversion of docstrings to markdown

MODPY_DISTV=	0.12
DISTNAME=	docstring-to-markdown-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}
REVISION=	1

CATEGORIES=	devel

# LGPL v2.1+
PERMIT_PACKAGE=	Yes

MODULES=	lang/python

MODPY_PI=	Yes
MODPY_PYBUILD=	setuptools

NO_TEST=	Yes

.include <bsd.port.mk>
