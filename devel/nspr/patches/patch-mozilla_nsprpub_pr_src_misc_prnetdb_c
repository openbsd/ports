$OpenBSD: patch-mozilla_nsprpub_pr_src_misc_prnetdb_c,v 1.8 2010/08/30 20:12:55 landry Exp $
--- mozilla/nsprpub/pr/src/misc/prnetdb.c.orig	Tue Mar 30 21:01:53 2010
+++ mozilla/nsprpub/pr/src/misc/prnetdb.c	Sun Aug  1 13:16:28 2010
@@ -2051,7 +2051,12 @@ PR_IMPLEMENT(PRAddrInfo *) PR_GetAddrInfoByName(const 
          */
         hints.ai_socktype = SOCK_STREAM;
 
+	LOCK_DNS();
+
         rv = GETADDRINFO(hostname, NULL, &hints, &res);
+
+	UNLOCK_DNS();
+
         if (rv == 0)
             return (PRAddrInfo *) res;
 
