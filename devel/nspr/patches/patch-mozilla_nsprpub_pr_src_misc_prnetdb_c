$OpenBSD: patch-mozilla_nsprpub_pr_src_misc_prnetdb_c,v 1.2 2006/11/06 14:35:45 ajacoutot Exp $
--- mozilla/nsprpub/pr/src/misc/prnetdb.c.orig	Fri Jul  1 03:19:30 2005
+++ mozilla/nsprpub/pr/src/misc/prnetdb.c	Sat Nov  4 13:15:21 2006
@@ -2110,7 +2110,12 @@ PR_IMPLEMENT(PRAddrInfo *) PR_GetAddrInf
          */
         hints.ai_socktype = SOCK_STREAM;
 
+	LOCK_DNS();
+
         rv = GETADDRINFO(hostname, NULL, &hints, &res);
+
+	UNLOCK_DNS();
+
         if (rv == 0)
             return (PRAddrInfo *) res;
 
