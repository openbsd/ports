COMMENT =	flexible and extensible dispatch

MODULES =	cpan
PKG_ARCH =	*
DISTNAME =	Path-Dispatcher-1.08
CATEGORIES =	devel

# perl
PERMIT_PACKAGE =	Yes

CPAN_AUTHOR =		ETHER

CONFIGURE_STYLE =	modbuild

BUILD_DEPENDS =	devel/p5-Module-Build-Tiny \
		${RUN_DEPENDS}
RUN_DEPENDS =	devel/p5-MooX-TypeTiny \
		devel/p5-Type-Tiny
TEST_DEPENDS =	devel/p5-Test-Fatal

.include <bsd.port.mk>
