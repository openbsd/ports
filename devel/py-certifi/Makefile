COMMENT=	TLS CA support for Python, patched to use /etc/ssl/cert.pem

MODPY_DISTV=	2025.10.5
DISTNAME=	certifi-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}
CATEGORIES=	devel security

HOMEPAGE=	https://certifi.io/

# ISC
PERMIT_PACKAGE=	Yes

MODULES=	lang/python
MODPY_PYBUILD=	setuptools
MODPY_PI=	Yes

.include <bsd.port.mk>
