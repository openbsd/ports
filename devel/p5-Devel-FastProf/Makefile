COMMENT=	per-line quick profiler for perl
CATEGORIES=	devel
DISTNAME=	Devel-FastProf-0.08
REVISION=	5
MODULES=	cpan

# perl
PERMIT_PACKAGE=	Yes

WANTLIB += c perl

RUN_DEPENDS=	devel/p5-Sort-Key

.include <bsd.port.mk>
