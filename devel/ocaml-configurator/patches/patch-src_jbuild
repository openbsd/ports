$OpenBSD: patch-src_jbuild,v 1.2 2021/12/01 03:29:11 daniel Exp $

support for dune 2.x

Index: src/jbuild
--- src/jbuild.orig
+++ src/jbuild
@@ -1,9 +0,0 @@
-(library
- ((name        configurator)
-  (public_name configurator)
-  (flags (:standard -safe-string))
-  (libraries (base stdio unix))
-  (preprocess no_preprocessing)))
-
-
-(jbuild_version 1)
