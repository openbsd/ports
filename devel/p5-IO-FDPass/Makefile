COMMENT =	pass a file descriptor over a socket

MODULES =	cpan
DISTNAME =	IO-FDPass-1.3
CATEGORIES =	devel

# perl
PERMIT_PACKAGE =	Yes

WANTLIB += c perl

BUILD_DEPENDS =	devel/p5-Canary-Stability

.include <bsd.port.mk>
