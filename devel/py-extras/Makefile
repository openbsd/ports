COMMENT=		useful extra bits for Python

MODPY_DISTV=	1.0.0
DISTNAME=		extras-${MODPY_DISTV}
PKGNAME=		py-${DISTNAME}
CATEGORIES=		devel
REVISION=		8

# MIT
PERMIT_PACKAGE=	Yes

MODULES=		lang/python

MODPY_PI=		Yes
MODPY_PYBUILD=		setuptools

TEST_DEPENDS=		devel/py-testtools

.include <bsd.port.mk>
