COMMENT =		simplify usage of decorators

MODPY_DISTV =	5.1.1
DISTNAME =		decorator-${MODPY_DISTV}
PKGNAME =		py-${DISTNAME}
REVISION =		2

CATEGORIES =		devel

# BSD
PERMIT_PACKAGE =	Yes

MODULES =		lang/python

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools
MODPY_PYTEST_ARGS =	src/tests/*.py

.include <bsd.port.mk>
