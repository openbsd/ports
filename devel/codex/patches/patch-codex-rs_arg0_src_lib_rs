Index: codex-rs/arg0/src/lib.rs
--- codex-rs/arg0/src/lib.rs.orig
+++ codex-rs/arg0/src/lib.rs
@@ -164,6 +164,8 @@ where
                         .as_ref()
                         .and_then(|path_entry| path_entry.paths().codex_linux_sandbox_exe.clone())
                 })
+            } else if cfg!(target_os = "openbsd") {
+                current_exe.or(Some(PathBuf::from("${PREFIX}/bin/codex")))
             } else {
                 None
             },
@@ -280,7 +282,8 @@ pub fn prepend_path_entry_for_codex_aliases() -> std::
         #[cfg(unix)]
         EXECVE_WRAPPER_ARG0,
     ] {
-        let exe = std::env::current_exe()?;
+        let exe = std::env::current_exe()
+            .unwrap_or_else(|_| PathBuf::from("${PREFIX}/bin/codex"));
 
         #[cfg(unix)]
         {
