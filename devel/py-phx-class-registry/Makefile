COMMENT =		python lib to register and instantiate classes

MODPY_DISTV =	4.1.0
DISTNAME =		phx-class-registry-${MODPY_DISTV}
PKGNAME =		py-${DISTNAME}
CATEGORIES =		devel
REVISION =		0

# MIT
PERMIT_PACKAGE = 	Yes

MODULES =		lang/python
MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools

RUN_DEPENDS =		devel/py-setuptools

.include <bsd.port.mk>
