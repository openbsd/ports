Index: src/libs/utils/crashreporting.cpp
--- src/libs/utils/crashreporting.cpp.orig
+++ src/libs/utils/crashreporting.cpp
@@ -262,7 +262,7 @@ void addCrashReportSettingsUi(QWidget *parent, Layouti
         auto crashButton = new QPushButton("CRASH!!!");
         QObject::connect(crashButton, &QPushButton::clicked, [] {
             // do a real crash
-            volatile int *a = reinterpret_cast<volatile int *>(NULL);
+            volatile int *a = reinterpret_cast<volatile int *>(0);
             *a = 1;
         });
         crashDetails.addItem(crashButton);
