COMMENT=		concise expressive creations of Class::Meta classes

MODULES=		cpan
PKG_ARCH=		*
DISTNAME =		Class-Meta-Express-0.14
CATEGORIES=		devel

# perl
PERMIT_PACKAGE=	Yes

BUILD_DEPENDS=		${RUN_DEPENDS}
RUN_DEPENDS=		devel/p5-Class-Meta>=0.63

CONFIGURE_STYLE= 	modbuild

.include <bsd.port.mk>
