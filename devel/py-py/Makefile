COMMENT=		cross-python path, ini-parsing, io, code, log facilities

MODPY_DISTV =	1.11.0
REVISION=		4
DISTNAME=		py-${MODPY_DISTV}
PKGNAME=		py-${DISTNAME}

CATEGORIES=		devel

# MIT
PERMIT_PACKAGE=	Yes

MODULES=		lang/python

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools_scm

.include <bsd.port.mk>
