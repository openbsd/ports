Use javaPathHelper

Index: d2j-dex-recompute-checksum.sh
--- d2j-dex-recompute-checksum.sh.orig
+++ d2j-dex-recompute-checksum.sh
@@ -45,4 +45,6 @@ else
     done
 fi
 
-java -Xms512m -Xmx2048m -classpath "${_classpath}" "com.googlecode.dex2jar.tools.DexRecomputeChecksum" "$@"
+JAVA_CMD=$(javaPathHelper -c dex2jar)
+
+${JAVA_CMD} -Xms512m -Xmx2048m -classpath "${_classpath}" "com.googlecode.dex2jar.tools.DexRecomputeChecksum" "$@"
