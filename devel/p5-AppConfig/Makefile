COMMENT=	bundle for reading and parsing config files

MODULES=	cpan
PKG_ARCH=	*
DISTNAME =	AppConfig-1.71
CATEGORIES=	devel
REVISION =	0

# perl
PERMIT_PACKAGE=	Yes

.ifmake test
PORTHOME=       ${HOME}
.endif

.include <bsd.port.mk>
