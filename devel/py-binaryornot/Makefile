COMMENT=	check if a file is binary or text

MODPY_DISTV=	0.5.0
GH_ACCOUNT=	binaryornot
GH_PROJECT=	binaryornot
GH_TAGNAME=	v${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}

CATEGORIES=	devel

# MIT
PERMIT_PACKAGE=	Yes

MODULES=	lang/python
MODPY_PYBUILD=	hatchling

TEST_DEPENDS=	devel/py-hypothesis

.include <bsd.port.mk>
