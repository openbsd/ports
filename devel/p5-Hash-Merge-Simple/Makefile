COMMENT =		recursively merge two or more hashes, simply

MODULES =		cpan
PKG_ARCH =		*
DISTNAME =		Hash-Merge-Simple-0.052
CATEGORIES =		devel

# Perl
PERMIT_PACKAGE =	Yes

RUN_DEPENDS =		devel/p5-Clone

TEST_DEPENDS =		devel/p5-Test-Deep

.include <bsd.port.mk>
