$OpenBSD: patch-common_autoconf_generated-configure_sh,v 1.1 2019/01/21 15:54:59 kurt Exp $

Index: common/autoconf/generated-configure.sh
--- common/autoconf/generated-configure.sh.orig
+++ common/autoconf/generated-configure.sh
@@ -16257,7 +16257,7 @@ $as_echo_n "checking flags for boot jdk java command f
   # Maximum amount of heap memory.
   # Maximum stack size.
   if test "x$BOOT_JDK_BITS" = x32; then
-    JVM_MAX_HEAP=1100M
+    JVM_MAX_HEAP=768M
     STACK_SIZE=768
   else
     # Running Javac on a JVM on a 64-bit machine, takes more space since 64-bit
