$OpenBSD: patch-src_hotspot_cpu_aarch64_templateTable_aarch64_cpp,v 1.1 2021/10/31 21:16:41 kurt Exp $

Fix implicit conversion failures from:
	long -> int64_t
	intptr_t -> int64_t

Index: src/hotspot/cpu/aarch64/templateTable_aarch64.cpp
--- src/hotspot/cpu/aarch64/templateTable_aarch64.cpp.orig
+++ src/hotspot/cpu/aarch64/templateTable_aarch64.cpp
@@ -1737,7 +1737,7 @@ void TemplateTable::float_cmp(bool is_float, int unord
   } else {
     // we want -1 for less than, 0 for equal and 1 for unordered or
     // greater than.
-    __ mov(r0, 1L);
+    __ mov(r0, (int64_t)1L);
     // for FP HI tests greater than or unordered
     __ br(Assembler::HI, done);
     // install 0 for EQ otherwise ~0
@@ -3613,7 +3613,7 @@ void TemplateTable::_new() {
     if (UseBiasedLocking) {
       __ ldr(rscratch1, Address(r4, Klass::prototype_header_offset()));
     } else {
-      __ mov(rscratch1, (intptr_t)markOopDesc::prototype());
+      __ mov(rscratch1, (int64_t)markOopDesc::prototype());
     }
     __ str(rscratch1, Address(r0, oopDesc::mark_offset_in_bytes()));
     __ store_klass_gap(r0, zr);  // zero klass gap for compressed oops
