Fix SIGBUS on sparc64 w/gcc 16

Index: make/hotspot/lib/JvmOverrideFiles.gmk
--- make/hotspot/lib/JvmOverrideFiles.gmk.orig
+++ make/hotspot/lib/JvmOverrideFiles.gmk
@@ -207,6 +207,9 @@ else ifeq ($(OPENJDK_TARGET_OS), bsd)
   BUILD_LIBJVM_loopTransform.cpp_CXXFLAGS := $(CXX_O_FLAG_NONE)
   ifneq ($(DEBUG_LEVEL), slowdebug)
     BUILD_LIBJVM_unsafe.cpp_CXXFLAGS := -O1
+    ifeq ($(TOOLCHAIN_TYPE), gcc)
+      BUILD_LIBJVM_sparsePRT.cpp_CXXFLAGS := -O1
+    endif
   endif
 
   ifeq ($(TOOLCHAIN_TYPE), gcc)
