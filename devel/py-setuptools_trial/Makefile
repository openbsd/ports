COMMENT=	setuptools plugin for unit tests with Twisted's trial

MODPY_DISTV=	0.6.0
DISTNAME=	setuptools_trial-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}
REVISION=	10

CATEGORIES=	devel

# BSD
PERMIT_PACKAGE=	Yes

MODULES=	lang/python

MODPY_PI=	Yes
MODPY_PYBUILD=	setuptools

RUN_DEPENDS =	devel/py-setuptools

# lots of pytest modules
NO_TEST=	Yes

.include <bsd.port.mk>
