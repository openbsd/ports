$OpenBSD: patch-old-locale_cabal,v 1.2 2018/01/22 21:42:27 kili Exp $

Dependency changed in the "online" version of the package description,
but not in the dist file.

Index: old-locale.cabal
--- old-locale.cabal.orig
+++ old-locale.cabal
@@ -31,5 +31,5 @@ Library
     exposed-modules:
         System.Locale
 
-    build-depends: base >= 4.2 && < 4.9
+    build-depends: base >= 4.2 && < 5
     ghc-options: -Wall
