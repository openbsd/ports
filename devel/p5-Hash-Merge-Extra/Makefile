COMMENT =		collection of extra behaviors for Hash::Merge

MODULES =		cpan
PKG_ARCH =		*
DISTNAME =		Hash-Merge-Extra-0.06
CATEGORIES =		devel

# Perl
PERMIT_PACKAGE =	Yes

RUN_DEPENDS =		devel/p5-Hash-Merge

.include <bsd.port.mk>
