COMMENT =	python module to interface with the pkg-config tool

MODPY_DISTV =		1.6.0
DISTNAME =		pkgconfig-${MODPY_DISTV}
PKGNAME =		py-${DISTNAME}
CATEGORIES =		devel

HOMEPAGE =		https://github.com/matze/pkgconfig

MAINTAINER =		Martin Reindl <martin@catai.org>

# MIT
PERMIT_PACKAGE =	Yes

MODULES =	lang/python
MODPY_PI =	Yes
MODPY_PYBUILD =	poetry-core

.include <bsd.port.mk>
