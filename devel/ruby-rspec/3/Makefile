    SUBDIR =
    SUBDIR += core,${FLAVOR}
    SUBDIR += expectations,${FLAVOR}
    SUBDIR += its,${FLAVOR}
    SUBDIR += mocks,${FLAVOR}
    SUBDIR += rspec,${FLAVOR}
    SUBDIR += support,${FLAVOR}

.include <bsd.port.subdir.mk>
