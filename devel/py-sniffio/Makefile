COMMENT =	sniff out which async library is being used

MODPY_DISTV =	1.3.1
DISTNAME=	sniffio-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}
REVISION=	0

CATEGORIES=	devel

# MIT or Apache 2.0
PERMIT_PACKAGE=	Yes

MODULES=	lang/python

MODPY_PI=	Yes
MODPY_PYBUILD=	setuptools_scm

TEST_DEPENDS=	devel/py-curio

.include <bsd.port.mk>
