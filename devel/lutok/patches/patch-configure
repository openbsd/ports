Don't clutter build logs.
Index: configure
--- configure.orig
+++ configure
@@ -20933,7 +20933,6 @@ fi
                      -Wcast-qual \
                      -Wextra \
                      -Wpointer-arith \
-                     -Wredundant-decls \
                      -Wreturn-type \
                      -Wshadow \
                      -Wsign-compare \
@@ -22122,19 +22121,6 @@ fi
             break
         fi
     done
-
-    if test "${lua_found}" = no
-then :
-
-else case e in #(
-  e)
-        LUA_CFLAGS=$(${PKG_CONFIG} --cflags ${lua_found})
-
-        LUA_LIBS=$(${PKG_CONFIG} --libs ${lua_found})
-
-         ;;
-esac
-fi
 
     if test "${lua_found}" = no
 then :
