COMMENT =		arrays of booleans

# py-bitstring needs updating to 4.3.0 (not released at time of
# writing) before updating to bitarray >= 3.
MODPY_EGG_VERSION =	2.9.3
DISTNAME =		bitarray-${MODPY_EGG_VERSION}
PKGNAME =		py-${DISTNAME}

CATEGORIES =		devel

HOMEPAGE =		https://github.com/ilanschnell/bitarray

# PSF
PERMIT_PACKAGE =	Yes

MODULES =		lang/python

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools

FLAVORS =		python3
FLAVOR =		python3

.include <bsd.port.mk>
