COMMENT=		simple base package from which to inherit

DISTNAME =		Package-New-0.09

CATEGORIES=		devel

# BSD
PERMIT_PACKAGE=	Yes

MODULES=		cpan
PKG_ARCH=		*

RUN_DEPENDS =		devel/p5-Devel-Hide

.include <bsd.port.mk>
