COMMENT=		library implementing the 'SemVer' scheme

MODPY_DISTV=	2.10.0

DISTNAME=		semantic_version-${MODPY_DISTV}
PKGNAME=		py-${DISTNAME}
REVISION=		3

CATEGORIES=		devel

HOMEPAGE=		https://github.com/rbarrois/python-semanticversion

# BSD
PERMIT_PACKAGE=		Yes

MODULES=		lang/python

MODPY_PI=		Yes
MODPY_PYBUILD=		setuptools

.include <bsd.port.mk>
