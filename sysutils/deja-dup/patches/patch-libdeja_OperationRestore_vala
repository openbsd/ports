$OpenBSD: patch-libdeja_OperationRestore_vala,v 1.1 2019/04/29 18:15:47 jasper Exp $

Index: libdeja/OperationRestore.vala
--- libdeja/OperationRestore.vala.orig
+++ libdeja/OperationRestore.vala
@@ -31,11 +31,7 @@ public class OperationRestore : Operation
       return this._restore_files;
     }
     construct {
-      foreach (File f in this._restore_files)
-        f.unref();
-      this._restore_files = value.copy();
-      foreach (File f in this._restore_files)
-        f.ref();
+      this._restore_files = value.copy_deep ((CopyFunc) Object.ref);
     }
   }
   
