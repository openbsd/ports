$OpenBSD: patch-install_rb,v 1.1 2013/03/08 16:12:47 ajacoutot Exp $

Don't gzip(1) the manual pages.

--- install.rb.orig	Tue Nov 20 00:46:29 2012
+++ install.rb	Thu Mar  7 11:29:26 2013
@@ -148,9 +148,6 @@ def do_man(man, strip = 'man/')
       FileUtils.chmod(0755, om)
       FileUtils.install(mf, omf, {:mode => 0644, :preserve => true, :verbose => true})
     end
-    gzip = %x{which gzip}
-    gzip.chomp!
-    %x{#{gzip} -f #{omf}}
   end
 end
 
