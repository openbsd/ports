Index: src/mhistory.c
--- src/mhistory.c.orig
+++ src/mhistory.c
@@ -58,7 +58,7 @@
 
 int history_write(mconfig *conf, mlist *l, char *subpath) {
 	char filename[255];
-	gzFile *fd;
+	gzFile fd;
 
 	/* protect buffer */
 	if (subpath) {
