COMMENT=	measure timing and throughput of code blocks

CATEGORIES=	sysutils devel

MODPY_DISTV=	4.2.2
DISTNAME=	about_time-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}

# MIT
PERMIT_PACKAGE=	yes

MODULES=	lang/python
MODPY_PYBUILD=	setuptools
MODPY_PI=	Yes

TEST_DEPENDS=	devel/py-test-cov

.include <bsd.port.mk>
