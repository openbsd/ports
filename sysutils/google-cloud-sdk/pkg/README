$OpenBSD: README,v 1.1.1.1 2016/01/30 18:07:48 ajacoutot Exp $

+-----------------------------------------------------------------------
| Running ${FULLPKGNAME} on OpenBSD
+-----------------------------------------------------------------------

Running the Google Cloud SDK requires the following to be set in the
environment:
    export PATH=${PATH}:${PREFIX}/google-cloud-sdk/bin

Note that for obvious reasons, the "docker" subcommand and kubectl are not
supported on OpenBSD for managing a GKE cluster.
