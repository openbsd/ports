Index: metricbeat/metricbeat.yml
--- metricbeat/metricbeat.yml.orig
+++ metricbeat/metricbeat.yml
@@ -120,11 +120,11 @@ output.elasticsearch:
 
 # Configure processors to enhance or manipulate events generated by the beat.
 
-processors:
-  - add_host_metadata: ~
-  - add_cloud_metadata: ~
-  - add_docker_metadata: ~
-  - add_kubernetes_metadata: ~
+#processors:
+  #- add_host_metadata: ~
+  #- add_cloud_metadata: ~
+  #- add_docker_metadata: ~
+  #- add_kubernetes_metadata: ~
 
 
 # ================================== Logging ===================================
