$OpenBSD: patch-filebeat_filebeat_yml,v 1.8 2020/04/04 02:52:58 pvk Exp $

add_host_metadata processor not yet implemented for OpenBSD

Index: filebeat/filebeat.yml
--- filebeat/filebeat.yml.orig
+++ filebeat/filebeat.yml
@@ -26,7 +26,8 @@ filebeat.inputs:
   # Paths that should be crawled and fetched. Glob based paths.
   paths:
     - /var/log/*.log
-    #- c:\programdata\elasticsearch\logs\*
+    - /var/log/authlog
+    - /var/log/daemon
 
   # Exclude lines. A list of regular expressions to match. It drops the lines that are
   # matching any regular expression from the list.
@@ -66,6 +67,8 @@ filebeat.inputs:
 #============================= Filebeat modules ===============================
 
 filebeat.config.modules:
+  # Disable modules by default
+  enabled: false
   # Glob pattern for configuration loading
   path: ${path.config}/modules.d/*.yml
 
@@ -176,11 +179,11 @@ output.elasticsearch:
 
 # Configure processors to enhance or manipulate events generated by the beat.
 
-processors:
-  - add_host_metadata: ~
-  - add_cloud_metadata: ~
-  - add_docker_metadata: ~
-  - add_kubernetes_metadata: ~
+#processors:
+  #- add_host_metadata: ~
+  #- add_cloud_metadata: ~
+  #- add_docker_metadata: ~
+  #- add_kubernetes_metadata: ~
 
 #================================ Logging =====================================
 
