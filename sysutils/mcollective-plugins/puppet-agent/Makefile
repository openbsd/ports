# $OpenBSD: Makefile,v 1.14 2015/06/10 13:30:07 jasper Exp $

COMMENT=		MCollective agent to manage the Puppet agent

GH_ACCOUNT=		puppetlabs
GH_PROJECT=		mcollective-puppet-agent
GH_TAGNAME=		1.10.0
DISTNAME=		${GH_PROJECT}-${GH_TAGNAME}

.include <bsd.port.mk>
