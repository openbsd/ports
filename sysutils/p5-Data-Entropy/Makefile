COMMENT =	entropy (randomness) management

DISTNAME =	Data-Entropy-0.008

CATEGORIES =	sysutils

# Perl
PERMIT_PACKAGE =	Yes

MODULES =		cpan

RUN_DEPENDS =		devel/p5-Module-Build \
			devel/p5-Params-Classify \
			security/p5-Crypt-Rijndael \
			security/p5-Crypt-URandom>=0.36 \
			sysutils/p5-Data-Float>=0.008 \
			www/p5-HTTP-Lite>=2.2
TEST_DEPENDS =		devel/p5-Test-Pod>=1.00 \
			devel/p5-Test-Pod-Coverage

PKG_ARCH =		*

.include <bsd.port.mk>
