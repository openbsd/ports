$OpenBSD: patch-src_md5_h,v 1.1 2003/12/16 10:04:37 sturm Exp $
--- src/md5.h.orig	2003-12-14 11:32:28.000000000 -0800
+++ src/md5.h	2003-12-14 11:32:47.000000000 -0800
@@ -27,6 +27,6 @@ void MD5Transform(uint32 buf[4], uint32 
  * This is needed to make RSAREF happy on some MS-DOS compilers.
  */
 /* TODO: why did I have to add an _ ? */
-typedef struct MD5Context MD5_CTX_;
+typedef struct MD5Context MD5_CTX;
 
 #endif /* !MD5_H */
