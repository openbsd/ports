Index: fsxfstools/fsxfsmount.c
--- fsxfstools/fsxfsmount.c.orig
+++ fsxfstools/fsxfsmount.c
@@ -488,8 +488,13 @@ int main( int argc, char * const argv[] )
 
 		goto on_error;
 	}
+#if defined(HAVE_LIBFUSE3)
 	fuse_unmount(
 	 fsxfsmount_fuse_handle );
+#else
+	fuse_unmount(
+	 mount_point, fsxfsmount_fuse_channel );
+#endif
 
 	fuse_destroy(
 	 fsxfsmount_fuse_handle );
