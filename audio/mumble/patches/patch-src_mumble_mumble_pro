$OpenBSD: patch-src_mumble_mumble_pro,v 1.7 2020/04/05 21:14:44 kn Exp $

Do not include nonexistent librt (part of libc).

Index: src/mumble/mumble.pro
--- src/mumble/mumble.pro.orig
+++ src/mumble/mumble.pro
@@ -539,9 +539,6 @@ unix {
     }
     
     must_pkgconfig(x11)
-    linux* {
-      LIBS *= -lrt
-    }
     LIBS *= -lXi
 
     !CONFIG(no-oss) {
