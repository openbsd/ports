Index: modcargo-crates/pipewire-sys-0.9.2/src/lib.rs
--- modcargo-crates/pipewire-sys-0.9.2/src/lib.rs.orig
+++ modcargo-crates/pipewire-sys-0.9.2/src/lib.rs
@@ -18,8 +18,8 @@ mod tests {
     #[test]
     fn init() {
         unsafe {
-            pw_init(std::ptr::null_mut(), std::ptr::null_mut());
-            pw_deinit();
+            pipewire_init(std::ptr::null_mut(), std::ptr::null_mut());
+            pipewire_deinit();
         }
     }
 }
