$OpenBSD: patch-configure_in,v 1.1.1.1 2001/01/09 20:59:36 naddy Exp $
--- configure.in.orig	Sat Jan  6 23:28:19 2001
+++ configure.in	Sat Jan  6 23:29:14 2001
@@ -160,8 +160,8 @@ irix*)
     ;;
 *)
     AM_PATH_AUDIOFILE(0.1.9, have_audiofile=yes, have_audiofile=no)
-    CFLAGS=$AUDIOFILE_CFLAGS
-    LIBS=$AUDIOFILE_LIBS
+    CFLAGS="$CFLAGS $AUDIOFILE_CFLAGS"
+    LIBS="$LIBS $AUDIOFILE_LIBS"
 
     if test "x$AUDIOFILE_LIBS" = "x"; then
         echo
@@ -204,8 +204,8 @@ AC_ARG_ENABLE(esd,
 
 if test "$enable_esd" = yes; then
     AM_PATH_ESD(0.2.8, have_esd=yes, have_esd=no)
-    CFLAGS=$ESD_CFLAGS
-    LIBS=$ESD_LIBS
+    CFLAGS="$CFLAGS $ESD_CFLAGS"
+    LIBS="$LIBS $ESD_LIBS"
     AC_CHECK_FUNCS(esd_get_latency)
     AM_CONDITIONAL(HAVE_ESD,test "x$have_esd" = xyes)
     found_sound=yes
