Index: mpris2/mpris2.c
--- mpris2/mpris2.c.orig
+++ mpris2/mpris2.c
@@ -456,7 +456,7 @@ imsg_dispatch(GIOChannel *chan, GIOCondition cond, gpo
 	struct imsg		 imsg;
 	struct ibuf		 ibuf;
 	struct player_event	 event;
-	ssize_t			 n;
+	int			 n;
 	size_t			 datalen;
 	const char		*msg;
 	char			 sha1buf[SHA1_DIGEST_STRING_LENGTH];
@@ -467,8 +467,8 @@ imsg_dispatch(GIOChannel *chan, GIOCondition cond, gpo
 		fatalx("pipe closed");
 
 	for (;;) {
-		if ((n = imsg_get(imsgbuf, &imsg)) == -1)
-			fatal("imsg_get");
+		if ((n = imsgbuf_get(imsgbuf, &imsg)) == -1)
+			fatal("imsgbuf_get");
 		if (n == 0)
 			break;
 
