Index: amused.c
--- amused.c.orig
+++ amused.c
@@ -123,8 +123,8 @@ main_dispatch_player(int sig, int event, void *d)
 	}
 
 	for (;;) {
-		if ((n = imsg_get(imsgbuf, &imsg)) == -1)
-			fatal("imsg_get");
+		if ((n = imsgbuf_get(imsgbuf, &imsg)) == -1)
+			fatal("imsgbuf_get");
 		if (n == 0)	/* No more messages. */
 			break;
 
