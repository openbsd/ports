cope with openbsd ancient nameser.h

Index: res/res_rtp_asterisk.c
--- res/res_rtp_asterisk.c.orig
+++ res/res_rtp_asterisk.c
@@ -9748,7 +9748,7 @@ static int store_stunaddr_resolved(const char *name, c
 		ast_debug_stun(2, "Record rr_type '%u' ttl: %d  data_size '%zu' from DNS query for stunaddr '%s'\n",
 			 rr_type, ttl, data_size, name);
 
-		if (rr_type == ns_t_a && data_size == 4) {
+		if (rr_type == T_A && data_size == 4) {
 			if (lock) {
 				ast_rwlock_wrlock(&stunaddr_lock);
 			}
