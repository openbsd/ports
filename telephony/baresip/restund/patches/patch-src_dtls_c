removed in https://github.com/baresip/re/commit/e11a4c584bdb0cb30dde9d4f3e8a7a5717506855

Index: src/dtls.c
--- src/dtls.c.orig
+++ src/dtls.c
@@ -180,7 +180,7 @@ static int dtls_listen_handler(const struct pl *val, v
 
 	(void)pl_strcpy(&cert, certpath, sizeof(certpath));
 
-	err = tls_alloc(&dl->tls, TLS_METHOD_DTLSV1, certpath, NULL);
+	err = tls_alloc(&dl->tls, TLS_METHOD_DTLS, certpath, NULL);
 	if (err) {
 		restund_warning("tls error: %m\n", err);
 		goto out;
