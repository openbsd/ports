Index: CMakeLists.txt
--- CMakeLists.txt.orig
+++ CMakeLists.txt
@@ -592,6 +592,10 @@ elseif(${CMAKE_SYSTEM_NAME} MATCHES "OpenBSD")
   list(APPEND SRCS
     src/net/bsd/brt.c
   )
+elseif(${CMAKE_SYSTEM_NAME} MATCHES "OpenBSD")
+  list(APPEND SRCS
+    src/net/bsd/brt.c
+  )
 elseif(${CMAKE_SYSTEM_NAME} MATCHES "Linux")
   list(APPEND SRCS
     src/net/linux/rt.c
