# $OpenBSD: Makefile,v 1.11 2007/10/27 17:56:55 deanna Exp $

     SUBDIR += app_conference
     SUBDIR += asterisk
     SUBDIR += asterisk-native-sounds
     SUBDIR += asterisk-sounds
     SUBDIR += chan_unistim
     SUBDIR += fobbit
     SUBDIR += libosip2
     SUBDIR += ohphone
     SUBDIR += p5-Net-SIP
     SUBDIR += p5-asterisk
     SUBDIR += pjsua
     SUBDIR += siproxd

.include <bsd.port.subdir.mk>
