typo ? and we want to enable bcg729 without building the bundled version

Index: external/linphone-sdk/cmake/Options.cmake
--- external/linphone-sdk/cmake/Options.cmake.orig
+++ external/linphone-sdk/cmake/Options.cmake
@@ -223,9 +223,9 @@ endif()
 set(ENABLE_JSONCPP ON CACHE BOOL "" FORCE)
 
 if(ENABLE_G729 OR ENABLE_G729B_CNG)
-	set(BUILD_BCG729 ON CACHE BOOL "" FORCE)
+	set(BUILD_BCG729 OFF CACHE BOOL "" FORCE)
 else()
-	set(BUILD_BC729 OFF CACHE BOOL "" FORCE)
+	set(BUILD_BCG729 OFF CACHE BOOL "" FORCE)
 endif()
 
 
