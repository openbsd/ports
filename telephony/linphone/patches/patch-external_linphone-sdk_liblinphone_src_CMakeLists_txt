Index: external/linphone-sdk/liblinphone/src/CMakeLists.txt
--- external/linphone-sdk/liblinphone/src/CMakeLists.txt.orig
+++ external/linphone-sdk/liblinphone/src/CMakeLists.txt
@@ -50,10 +50,6 @@ if(LibXml2_FOUND)
 	list(APPEND PUBLIC_LINK_LIBS ${LibXml2_TARGET})
 endif()
 
-if(HAVE_DLOPEN)
-	list(APPEND LINK_LIBS dl)
-endif()
-
 if(XercesC_FOUND)
 	list(APPEND LINK_LIBS ${XercesC_TARGET})
 	if (APPLE AND NOT IOS)
