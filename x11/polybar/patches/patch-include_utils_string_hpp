Revert part of https://github.com/polybar/polybar/pull/2958

Index: include/utils/string.hpp
--- include/utils/string.hpp.orig
+++ include/utils/string.hpp
@@ -85,7 +85,7 @@ string trim(string&& value, const char& needle = ' ');
 size_t char_len(const string& value);
 string utf8_truncate(string&& value, size_t len);
 [[nodiscard]] bool utf8_to_ucs4(const string& src, unicode_charlist& result_list);
-size_t ucs4_to_utf8(std::array<char, 5>& utf8, unsigned int ucs);
+size_t ucs4_to_utf8(char* utf8, unsigned int ucs);
 
 string join(const vector<string>& strs, const string& delim);
 vector<string> split(const string& s, char delim);
