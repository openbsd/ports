$OpenBSD: patch-Makefile_in,v 1.6 2008/04/16 07:41:35 ajacoutot Exp $
--- Makefile.in.orig	Tue Apr 15 18:26:09 2008
+++ Makefile.in	Wed Apr 16 09:10:03 2008
@@ -926,11 +926,6 @@ dist_noinst_DATA = \
 	tests/urgent.c
 
 dist_doc_DATA = \
-	COMPLIANCE \
-	README \
-	AUTHORS \
-	CHANGELOG \
-	COPYING \
 	data/rc.xsd \
 	data/menu.xsd \
 	doc/rc-mouse-focus.xml
@@ -3101,12 +3096,12 @@ uninstall-dist_pixmapDATA:
 	done
 install-dist_rcDATA: $(dist_rc_DATA)
 	@$(NORMAL_INSTALL)
-	test -z "$(rcdir)" || $(mkdir_p) "$(DESTDIR)$(rcdir)"
+	test -z "$(docdir)" || $(mkdir_p) "$(DESTDIR)$(docdir)"
 	@list='$(dist_rc_DATA)'; for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
 	  f=$(am__strip_dir) \
-	  echo " $(dist_rcDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(rcdir)/$$f'"; \
-	  $(dist_rcDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(rcdir)/$$f"; \
+	  echo " $(dist_rcDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(docdir)/$$f'"; \
+	  $(dist_rcDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(docdir)/$$f"; \
 	done
 
 uninstall-dist_rcDATA:
