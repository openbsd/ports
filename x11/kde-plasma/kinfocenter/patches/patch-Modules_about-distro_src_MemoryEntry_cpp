Index: Modules/about-distro/src/MemoryEntry.cpp
--- Modules/about-distro/src/MemoryEntry.cpp.orig
+++ Modules/about-distro/src/MemoryEntry.cpp
@@ -38,6 +38,11 @@ qlonglong MemoryEntry::calculateTotalRam()
     sysctlbyname("hw.physmem", &memory, &len, NULL, 0);
 
     ret = memory;
+#elif defined(Q_OS_OPENBSD)
+    long phys_pages = sysconf(_SC_PHYS_PAGES);
+    long pagesize = sysconf(_SC_PAGESIZE);
+    if (phys_pages != -1 && pagesize != -1)
+        ret = ((uint64_t)phys_pages * (uint64_t)pagesize / 1024);
 #endif
     return ret;
 }
