# kde-fingerprint (kscreenlocker) settings for pam(3)
# XXX No fingerprint support on OpenBSD

auth		include		system

account		required	pam_nologin.so
account		include		system

password	include		system

session		include		system
