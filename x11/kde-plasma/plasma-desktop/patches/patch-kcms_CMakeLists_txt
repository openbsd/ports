Index: kcms/CMakeLists.txt
--- kcms/CMakeLists.txt.orig
+++ kcms/CMakeLists.txt
@@ -25,7 +25,7 @@ add_subdirectory(kded)
 add_subdirectory(runners)
 add_subdirectory(spellchecking)
 add_subdirectory(qtquicksettings)
-add_subdirectory(touchscreen)
+#add_subdirectory(touchscreen)
 
 add_subdirectory(workspaceoptions)
 if (KF6Baloo_FOUND)
@@ -37,10 +37,10 @@ if (BUILD_KCM_MOUSE_KWIN_WAYLAND OR BUILD_KCM_MOUSE_X1
     add_subdirectory(mouse)
 endif()
 
-if (BUILD_KCM_TOUCHPAD_KWIN_WAYLAND OR BUILD_KCM_TOUCHPAD_X11)
-    add_subdirectory(touchpad)
-endif()
-
-if(BUILD_KCM_TABLET)
-    add_subdirectory(tablet)
-endif()
+#if (BUILD_KCM_TOUCHPAD_KWIN_WAYLAND OR BUILD_KCM_TOUCHPAD_X11)
+#    add_subdirectory(touchpad)
+#endif()
+#
+#if(BUILD_KCM_TABLET)
+#    add_subdirectory(tablet)
+#endif()
