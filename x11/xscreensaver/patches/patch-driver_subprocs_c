64bit time_t

Index: driver/subprocs.c
--- driver/subprocs.c.orig
+++ driver/subprocs.c
@@ -1128,9 +1128,9 @@ spawn_screenhack (saver_screen_info *ssi)
               if ((p->verbose_p > 1 ||
                    (p->verbose_p && !printed_p)) &&
                   cycle_at - old > 2)
-                fprintf (stderr, "%s: %d: offsetting cycle time by %ld sec\n",
+                fprintf (stderr, "%s: %d: offsetting cycle time by %lld sec\n",
                          blurb(), ssi->number,
-                         cycle_at - old);
+                         (long long)cycle_at - old);
             }
         }
 
