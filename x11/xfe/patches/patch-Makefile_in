$OpenBSD: patch-Makefile_in,v 1.13 2015/08/14 03:42:15 bcallah Exp $
--- Makefile.in.orig	Mon Aug 10 03:58:21 2015
+++ Makefile.in	Thu Aug 13 12:40:33 2015
@@ -571,17 +571,13 @@ uninstall-iconDATA:
 install-rcDATA: $(rc_DATA)
 	@$(NORMAL_INSTALL)
 	@list='$(rc_DATA)'; test -n "$(rcdir)" || list=; \
-	if test -n "$$list"; then \
-	  echo " $(MKDIR_P) '$(DESTDIR)$(rcdir)'"; \
-	  $(MKDIR_P) "$(DESTDIR)$(rcdir)" || exit 1; \
-	fi; \
 	for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
 	  echo "$$d$$p"; \
 	done | $(am__base_list) | \
 	while read files; do \
-	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rcdir)'"; \
-	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rcdir)" || exit $$?; \
+	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(prefix)/share/xfe'"; \
+	  $(INSTALL_DATA) $$files "$(DESTDIR)$(prefix)/share/xfe" || exit $$?; \
 	done
 
 uninstall-rcDATA:
