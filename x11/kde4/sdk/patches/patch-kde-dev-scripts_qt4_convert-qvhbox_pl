$OpenBSD: patch-kde-dev-scripts_qt4_convert-qvhbox_pl,v 1.1.1.1 2013/04/24 19:04:15 zhuk Exp $
--- kde-dev-scripts/qt4/convert-qvhbox.pl.orig	Sun Apr  7 13:27:40 2013
+++ kde-dev-scripts/qt4/convert-qvhbox.pl	Sun Apr  7 15:14:07 2013
@@ -7,7 +7,7 @@ use lib dirname( $0 );
 use functionUtilkde;
 use strict;
 
-open(my $F, q(find -name "*" |));
+open(my $F, "-|", qw(find . -type f));
 my $file;
 
 while ($file = <$F>) {
@@ -15,7 +15,7 @@ while ($file = <$F>) {
     next if functionUtilkde::excludeFile( $file);
 
     my $modified;
-    open(my $FILE, $file) or warn "We can't open file $file:$!\n";
+    open(my $FILE, "<", $file) or warn "We can't open file $file:$!\n";
     my @l = map {
         my $orig = $_;
 	s!#include <q3vbox.h>!!;
@@ -32,11 +32,11 @@ while ($file = <$F>) {
     } <$FILE>;
 
     if ($modified) {
-        open (my $OUT, ">$file");
+        open (my $OUT, ">", $file);
         print $OUT @l;
         close ($OUT);
         functionUtilkde::addIncludeInFile( $file, "kvbox.h");
     }
 
 }
-functionUtilkde::diffFile( "@ARGV" );
+functionUtilkde::diffFile( @ARGV );
