$OpenBSD: patch-CMakeLists_txt,v 1.1 2018/08/13 09:13:30 rsadowski Exp $

Index: CMakeLists.txt
--- CMakeLists.txt.orig
+++ CMakeLists.txt
@@ -6,7 +6,6 @@ find_package(KDE4 REQUIRED)
 include(KDE4Defaults)
 add_definitions (${QT_DEFINITIONS} ${KDE4_DEFINITIONS})
 
-find_package(LibKompareDiff2 REQUIRED)
 include_directories (${CMAKE_SOURCE_DIR} ${CMAKE_BINARY_DIR} ${KDE4_INCLUDES} ${LIBKOMPAREDIFF2_INCLUDE_DIR})
 
 add_subdirectory( doc )
