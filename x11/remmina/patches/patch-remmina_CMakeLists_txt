$OpenBSD: patch-remmina_CMakeLists_txt,v 1.8 2018/05/10 12:32:05 jasper Exp $

From 49bb97e49be7b6e55badaf5f1b44c3fe6e14c3f3 Mon Sep 17 00:00:00 2001
From: Antenore Gatta <antenore@simbiosi.org>
Date: Tue, 8 May 2018 23:16:43 +0200
Subject: [PATCH] Add intl support for OpenBSD and NetBSD. #1604

Index: remmina/CMakeLists.txt
--- remmina/CMakeLists.txt.orig
+++ remmina/CMakeLists.txt
@@ -200,8 +200,8 @@ if(GTK3_FOUND)
 endif()
 
 find_package(Intl)
-if(INTL_FOUND AND "${CMAKE_SYSTEM_NAME}" MATCHES "FreeBSD")
-    message(STATUS "FreeBSD detected, building with Intl")
+if(INTL_FOUND AND "${CMAKE_SYSTEM_NAME}" MATCHES "FreeBSD|OpenBSD|NetBSD")
+    message(STATUS "${CMAKE_SYSTEM_NAME} detected, building with Intl")
     include_directories(${Intl_INCLUDE_DIRS})
     target_link_libraries(remmina ${Intl_LIBRARIES})
 endif()
