Index: src/3rdparty/chromium/third_party/skia/gn/BUILDCONFIG.gn
--- src/3rdparty/chromium/third_party/skia/gn/BUILDCONFIG.gn.orig
+++ src/3rdparty/chromium/third_party/skia/gn/BUILDCONFIG.gn
@@ -63,6 +63,8 @@ is_fuchsia = current_os == "fuchsia"
 is_ios = current_os == "ios" || current_os == "tvos"
 is_tvos = current_os == "tvos"
 is_linux = current_os == "linux"
+is_bsd = current_os == "openbsd"
+is_openbsd = current_os == "openbsd"
 is_mac = current_os == "mac"
 is_wasm = current_os == "wasm"
 is_win = current_os == "win"
