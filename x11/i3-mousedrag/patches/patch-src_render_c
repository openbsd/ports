$OpenBSD: patch-src_render_c,v 1.1 2021/02/14 20:46:20 sthen Exp $

Index: src/render.c
--- src/render.c.orig
+++ src/render.c
@@ -46,7 +46,7 @@ void render_con(Con *con) {
         .y = con->rect.y,
         .children = con_num_children(con)};
 
-    DLOG("Rendering node %p / %s / layout %d / children %d\n", con, con->name,
+    DLOG("Rendering node %p / %s / layout %d / children %d\n", con, con->name ? con->name : "(null)",
          con->layout, params.children);
 
     int i = 0;
