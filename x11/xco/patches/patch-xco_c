Index: xco.c
--- xco.c.orig
+++ xco.c
@@ -29,6 +29,7 @@
 ************************************************************************/
 
 #include <stdio.h>
+#include <stdlib.h>
 #include <X11/Xlib.h>
 #include <X11/Xutil.h>
 #include <X11/keysym.h>
@@ -71,7 +72,6 @@ char *sbfg;			/* default foreground 			*/
 char *sbbg;			/* default background 			*/
 
 extern Display *ParseXArgs();
-extern char *malloc();
 char *GetColorName();
 
 main(csb, rgsb)
@@ -310,7 +310,7 @@ char *rgsb[];
 	  case ButtonPress:
 	    if (ev.xbutton.window == wnPrevious)
 		{
-	    	    XFreeColors(pdp, cm, rgco, cColorMax, NULL);
+	    	    XFreeColors(pdp, cm, rgco, cColorMax, 0);
 	    	    iBase += MaxColor - cColorScroll;
 	    	    XClearArea(pdp, wn, 0, 0, 0, 0, True);
 	    	    CreateColorWindows(pdp, wn);
@@ -318,7 +318,7 @@ char *rgsb[];
 		}
             else if (ev.xbutton.window == wnNext)
 		{
-		    XFreeColors(pdp, cm, rgco, cColorMax, NULL);
+		    XFreeColors(pdp, cm, rgco, cColorMax, 0);
 		    iBase += cColorScroll;
 		    XClearArea(pdp, wn, 0, 0, 0, 0, True);
 		    CreateColorWindows(pdp, wn);
