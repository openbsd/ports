Index: winpr/libwinpr/synch/test/CMakeLists.txt
--- winpr/libwinpr/synch/test/CMakeLists.txt.orig
+++ winpr/libwinpr/synch/test/CMakeLists.txt
@@ -20,9 +20,14 @@ set(${MODULE_PREFIX}_TESTS
     TestSynchAPC.c
 )
 
+# openbsd doesn't have proper posix timers
+if(OPENBSD)
+  list(REMOVE_ITEM ${MODULE_PREFIX}_TESTS TestSynchTimerQueue.c)
+endif()
+
 create_test_sourcelist(${MODULE_PREFIX}_SRCS ${${MODULE_PREFIX}_DRIVER} ${${MODULE_PREFIX}_TESTS})
 
-if(FREEBSD)
+if(EPOLLSHIM)
   include_directories(SYSTEM ${EPOLLSHIM_INCLUDE_DIR})
 endif()
 
