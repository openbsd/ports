$OpenBSD$

Index: src/3rdparty/chromium/third_party/swiftshader/src/Vulkan/VkDebug.cpp
--- src/3rdparty/chromium/third_party/swiftshader/src/Vulkan/VkDebug.cpp.orig
+++ src/3rdparty/chromium/third_party/swiftshader/src/Vulkan/VkDebug.cpp
@@ -33,7 +33,7 @@ namespace {
 
 bool IsUnderDebugger()
 {
-#if defined(PTRACE) && !defined(__APPLE__) && !defined(__MACH__)
+#if defined(PTRACE) && !defined(__APPLE__) && !defined(__MACH__) && !defined(__OpenBSD__)
 	static bool checked = false;
 	static bool res = false;
 
