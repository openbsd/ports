Index: src/3rdparty/chromium/net/cookies/cookie_monster.h
--- src/3rdparty/chromium/net/cookies/cookie_monster.h.orig
+++ src/3rdparty/chromium/net/cookies/cookie_monster.h
@@ -256,7 +256,7 @@ class NET_EXPORT CookieMonster : public CookieStore {
   // the CookieChangeCause mapping inside ChangeCauseMapping. New items (if
   // necessary) should be added at the end of the list, just before
   // DELETE_COOKIE_LAST_ENTRY.
-  enum DeletionCause {
+  enum DeletionCause : uint8_t {
     DELETE_COOKIE_EXPLICIT = 0,
     DELETE_COOKIE_OVERWRITE = 1,
     DELETE_COOKIE_EXPIRED = 2,
