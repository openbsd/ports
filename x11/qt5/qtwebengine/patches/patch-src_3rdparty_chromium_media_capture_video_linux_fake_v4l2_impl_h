Index: src/3rdparty/chromium/media/capture/video/linux/fake_v4l2_impl.h
--- src/3rdparty/chromium/media/capture/video/linux/fake_v4l2_impl.h.orig
+++ src/3rdparty/chromium/media/capture/video/linux/fake_v4l2_impl.h
@@ -35,7 +35,7 @@ class CAPTURE_EXPORT FakeV4L2Impl : public V4L2Capture
   // Implementation of V4L2CaptureDevice interface:
   int open(const char* device_name, int flags) override;
   int close(int fd) override;
-  int ioctl(int fd, int request, void* argp) override;
+  int ioctl(int fd, unsigned long request, void* argp) override;
   void* mmap(void* start,
              size_t length,
              int prot,
