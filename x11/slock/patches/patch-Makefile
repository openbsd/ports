$OpenBSD: patch-Makefile,v 1.4 2015/04/16 19:00:09 jung Exp $
--- Makefile.orig	Wed Apr 15 21:49:50 2015
+++ Makefile	Wed Apr 15 21:51:14 2015
@@ -15,8 +15,7 @@ options:
 	@echo "CC       = ${CC}"
 
 .c.o:
-	@echo CC $<
-	@${CC} -c ${CFLAGS} $<
+	${CC} -c ${CFLAGS} $<
 
 ${OBJ}: config.h config.mk
 
@@ -25,8 +24,7 @@ config.h:
 	@cp config.def.h $@
 
 slock: ${OBJ}
-	@echo CC -o $@
-	@${CC} -o $@ ${OBJ} ${LDFLAGS}
+	${CC} -o $@ ${OBJ} ${LDFLAGS}
 
 clean:
 	@echo cleaning
@@ -42,10 +40,7 @@ dist: clean
 
 install: all
 	@echo installing executable file to ${DESTDIR}${PREFIX}/bin
-	@mkdir -p ${DESTDIR}${PREFIX}/bin
-	@cp -f slock ${DESTDIR}${PREFIX}/bin
-	@chmod 755 ${DESTDIR}${PREFIX}/bin/slock
-	@chmod u+s ${DESTDIR}${PREFIX}/bin/slock
+	@${BSD_INSTALL_PROGRAM} slock ${DESTDIR}${PREFIX}/bin
 
 uninstall:
 	@echo removing executable file from ${DESTDIR}${PREFIX}/bin
