Index: CMakeLists.txt
--- CMakeLists.txt.orig
+++ CMakeLists.txt
@@ -58,6 +58,6 @@ if(BUILD_TESTING)
     add_subdirectory(tests)
 endif()
 
-ki18n_install(po)
+#ki18n_install(po)
 
 feature_summary(WHAT ALL FATAL_ON_MISSING_REQUIRED_PACKAGES)
