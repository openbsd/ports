Index: meson.build
--- meson.build.orig
+++ meson.build
@@ -59,7 +59,11 @@ peas_req_version = '2.0'
 peas_req = '>= @0@'.format(peas_req_version)
 peas_dep = dependency('libpeas-2', version: peas_req)
 
-sysprof_dep = dependency('sysprof-capture-4')
+if build_machine.system() == 'linux'
+  sysprof_dep = dependency('sysprof-capture-4')
+else
+  sysprof_dep = []
+endif
 
 project_c_args = []
 test_c_args = [
