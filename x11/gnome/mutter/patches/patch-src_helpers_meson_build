../mutter-49.1.1/src/helpers/meta-backlight-helper.c:42:2: error: "gsd-backlight-helper does not work on non-Linux"
   42 | #error "gsd-backlight-helper does not work on non-Linux

Index: src/helpers/meson.build
--- src/helpers/meson.build.orig
+++ src/helpers/meson.build
@@ -2,6 +2,8 @@ helpers_cdata = configuration_data()
 helpers_cdata.set('libexecdir', libexecdir)
 
 # backlight helper
+# Only used by MetaBacklightSysfs (Linux sysfs + udev).
+if have_libgudev
 configure_file(
   input: 'org.gnome.mutter.backlight-helper.policy.in',
   output: 'org.gnome.mutter.backlight-helper.policy',
@@ -18,6 +20,7 @@ executable('mutter-backlight-helper',
   install_dir: libexecdir,
   install: true,
 )
+endif
 
 # restart helper
 if have_x11
@@ -37,4 +40,4 @@ if have_x11
     install_dir: libexecdir,
     install: true,
   )
-endif
\ No newline at end of file
+endif
