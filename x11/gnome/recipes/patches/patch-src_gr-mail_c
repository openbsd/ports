$OpenBSD: patch-src_gr-mail_c,v 1.1 2017/11/02 11:22:38 jasper Exp $

https://bugzilla.gnome.org/show_bug.cgi?id=789812

patch-src_gr-mail_c
Index: src/gr-mail.c
--- src/gr-mail.c.orig
+++ src/gr-mail.c
@@ -284,7 +284,7 @@ window_handle_exported (GtkWindow  *window,
                         int fd;
                         int fd_in;
 
-                        fd = g_open (md->attachments[i], O_PATH | O_CLOEXEC);
+                        fd = g_open (md->attachments[i], O_CLOEXEC);
                         if (fd == -1) {
                                 g_warning ("Failed to open %s, skipping", md->attachments[i]);
                                 continue;
