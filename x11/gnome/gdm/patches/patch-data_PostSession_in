$OpenBSD: patch-data_PostSession_in,v 1.6 2021/02/12 17:01:18 ajacoutot Exp $

Index: data/PostSession.in
--- data/PostSession.in.orig
+++ data/PostSession.in
@@ -1,3 +1,17 @@
 #!/bin/sh
 
+# /etc/X11/xenodm/TakeConsole, fbtab(5)
+if [ -c /dev/drm0 ]; then
+    /sbin/chown root /dev/drm0
+fi
+if [ -c /dev/dri/card0 ]; then
+    /sbin/chown root /dev/dri/card0
+fi
+if [ -c /dev/drmR128 ]; then
+    /sbin/chown root /dev/drmR128
+fi
+if [ -c /dev/dri/renderD128 ]; then
+    /sbin/chown root /dev/dri/renderD128
+fi
+
 exit 0
