Index: spectrwm.c
--- spectrwm.c.orig
+++ spectrwm.c
@@ -382,7 +382,7 @@ uint32_t		swm_debug = 0
 #define SWM_CONF_WHITESPACE	" \t\n"
 
 #ifndef SWM_LIB
-#define SWM_LIB			"/usr/local/lib/libswmhack.so"
+#define SWM_LIB			"${PREFIX}/lib/libswmhack.so.${LIBswmhack_VERSION}"
 #endif
 
 char			**start_argv;
@@ -18911,7 +18911,9 @@ main(int argc, char *argv[])
 			bar_setup(r);
 
 #ifdef __OpenBSD__
-	if (pledge("stdio proc exec", NULL) == -1)
+	/* XXX rpath wpath had to be added for /dev/null;
+	       could probably unveil to restrict further */
+	if (pledge("stdio proc exec rpath wpath", NULL) == -1)
 		err(1, "pledge");
 #endif
 
