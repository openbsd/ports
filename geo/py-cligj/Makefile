COMMENT =		click params for command line interfaces to GeoJSON

MODPY_DISTV =	0.7.2
DISTNAME =		cligj-${MODPY_DISTV}
PKGNAME =		py-${DISTNAME}
REVISION =		4

CATEGORIES =		geo

# BSD
PERMIT_PACKAGE =	Yes

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools
MODULES =		lang/python

RUN_DEPENDS =		devel/py-click

.include <bsd.port.mk>
