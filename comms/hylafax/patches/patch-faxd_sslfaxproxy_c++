Index: faxd/sslfaxproxy.c++
--- faxd/sslfaxproxy.c++.orig
+++ faxd/sslfaxproxy.c++
@@ -31,7 +31,7 @@ printlog(FILE *fp, const char *fmt, ...)
   fflush(fp);
 }
 
-timeval currentTime() {
+static timeval currentTime() {
     timeval curTime;
     gettimeofday(&curTime, 0);
     return curTime;
@@ -94,6 +94,7 @@ sslFaxThread(void* sd)
 done:
     Sys::close(s->wfd);
     Sys::close(s->rfd);
+    free(sd);
     return (NULL);
 }
 
