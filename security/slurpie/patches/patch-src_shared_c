Index: src/shared.c
--- src/shared.c.orig
+++ src/shared.c
@@ -13,6 +13,6 @@ shared.c: shared stuff among slurpie.c and slurp.c.
 u_int strcpy_len( u_char *t, u_char *s )
 {
    u_char *low = s;
-   while( *(t++) = *(s++) );
+   while( (*(t++) = *(s++)) );
    return (s-low);
 }
