Index: Makefile.am
--- Makefile.am.orig
+++ Makefile.am
@@ -43,12 +43,6 @@ if DEBUG
 AM_CFLAGS+=-g -fno-omit-frame-pointer
 endif
 
-if OPTIMIZATION
-AM_CFLAGS+=-O3
-else
-AM_CFLAGS+=-O0
-endif
-
 endif
 
 if ADDRESS_SANITIZER
