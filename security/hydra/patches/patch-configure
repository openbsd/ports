Index: configure
--- configure.orig
+++ configure
@@ -200,6 +200,11 @@ fi
 STRIP="strip"
 echo
 
+if [ "$SYSS" = "OpenBSD" ]; then
+LIBDIRS="/usr/lib /usr/X11R6/lib /usr/local/lib"
+INCDIRS="/usr/include /usr/local/include"
+fi
+
 echo "Checking for zlib (libz/zlib.h) ..."
 for i in $INCDIRS; do
    if [ -f "$i/zlib.h" ]; then
@@ -1103,12 +1108,12 @@ echo "Checking for Freerdp3 (libfreerdp3/freerdp.h/lib
 
  for i in $LIBDIRS ; do
     if [ "X" = "X$WINPR3_PATH" ]; then
-        if [ -f "$i/libwinpr3.so" -o -f "$i/libwinpr3.dylib" -o -f "$i/libwinpr3.a" ]; then
+        if [ -f "$i/libwinpr3.so" -o -f "$i/libwinpr3.dylib" -o -f "$i/libwinpr3.a" -o -f "$i/libwinpr3.dll.a" ]; then
             WINPR3_PATH="$i"
         fi
     fi
     if [ "X" = "X$WINPR3_PATH" ]; then
-        TMP_LIB=`/bin/ls $i/libwinpr3.dll.a 2> /dev/null | grep winpr`
+        TMP_LIB=`/bin/ls $i/libwinpr3.so* 2> /dev/null | grep libwinpr3`
         if [ -n "$TMP_LIB" ]; then
           WINPR3_PATH="$i"
         fi
@@ -1595,7 +1600,7 @@ if [ -n "$FIREBIRD_PATH" -o \
             XLIBPATHS="-L/usr/lib -L/usr/local/lib"
         fi
     else
-        XLIBPATHS="-L/usr/lib -L/usr/local/lib -L/lib"
+        XLIBPATHS="-L/usr/X11R6/lib -L/usr/lib -L/usr/local/lib -L/lib"
     fi
 fi
 if [ -n "$MYSQL_IPATH" ]; then
