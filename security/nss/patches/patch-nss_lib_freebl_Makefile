$OpenBSD: patch-nss_lib_freebl_Makefile,v 1.1 2018/05/14 18:43:47 landry Exp $

Fix build on !x86
https://bugzilla.mozilla.org/show_bug.cgi?id=1459739#c2

Index: nss/lib/freebl/Makefile
--- nss/lib/freebl/Makefile.orig
+++ nss/lib/freebl/Makefile
@@ -535,11 +535,13 @@ ifeq (,$(filter-out i386 x386 x86 x86_64 aarch64,$(CPU
     # All intel architectures get the 64 bit version
     # With custom uint128 if necessary (faster than generic 32 bit version).
     ECL_SRCS += curve25519_64.c
-    VERIFIED_SRCS += Hacl_Curve25519.c FStar.c
+    VERIFIED_SRCS += Hacl_Curve25519.c
 else
     # All non intel architectures get the generic 32 bit implementation (slow!)
     ECL_SRCS += curve25519_32.c
 endif
+
+VERIFIED_SRCS += FStar.c
 
 #######################################################################
 # (5) Execute "global" rules. (OPTIONAL)                              #
