don't error out for newer 4.x.x LibreSSL versions (upstream only lists
versions released at the time, so this soon ends up out-of-sync after
LibreSSL updates).

Index: .cargo/vendor/openssl-sys/build/main.rs
--- .cargo/vendor/openssl-sys/build/main.rs.orig
+++ .cargo/vendor/openssl-sys/build/main.rs
@@ -430,6 +430,7 @@ See rust-openssl documentation for more information:
             (4, 1, _) => ('4', '1', 'x'),
             (4, 2, _) => ('4', '2', 'x'),
             (4, 3, _) => ('4', '3', 'x'),
+            (4, _, _) => ('4', '3', 'x'),
             _ => version_error(),
         };
 
