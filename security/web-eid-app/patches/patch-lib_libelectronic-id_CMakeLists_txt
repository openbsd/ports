Drop OpenSSL version spec to find LibreSSL

Index: lib/libelectronic-id/CMakeLists.txt
--- lib/libelectronic-id/CMakeLists.txt.orig
+++ lib/libelectronic-id/CMakeLists.txt
@@ -46,7 +46,7 @@ target_include_directories(${PROJECT_NAME}
   include
 )
 
-find_package(OpenSSL 3.0.0 REQUIRED)
+find_package(OpenSSL REQUIRED)
 
 target_link_libraries(${PROJECT_NAME}
   pcsc-cpp
