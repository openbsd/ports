$OpenBSD: patch-src_evp_c,v 1.2 2011/04/16 11:58:43 jasper Exp $
--- src/evp.c.orig	Sun Jan 22 20:16:35 2006
+++ src/evp.c	Fri Apr 15 21:09:25 2011
@@ -97,7 +97,7 @@ static int Ldigest(lua_State *L)		/** digest(c, s, [ra
   EVP_MD_CTX *c = Pget(L, 1);
   EVP_MD_CTX *d = NULL;
   unsigned char digest[EVP_MAX_MD_SIZE];
-  size_t written = 0;
+  unsigned int written = 0;
   
   if (lua_isstring(L, 2))
   {  
@@ -149,7 +149,7 @@ static int Lfdigest(lua_State *L)		/** digest(type, s,
   const char *s = luaL_checkstring(L, 2);
   const EVP_MD *type = EVP_get_digestbyname(type_name);
   unsigned char digest[EVP_MAX_MD_SIZE];
-  size_t written = 0;
+  unsigned int written = 0;
   
   if (type == NULL) {
     luaL_argerror(L, 1, "invalid digest type");
