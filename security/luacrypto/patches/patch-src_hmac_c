$OpenBSD: patch-src_hmac_c,v 1.2 2011/04/16 11:58:43 jasper Exp $
--- src/hmac.c.orig	Sun Jan 22 20:16:35 2006
+++ src/hmac.c	Fri Apr 15 21:09:25 2011
@@ -92,7 +92,7 @@ static int Ldigest(lua_State *L)		/** digest(c, s, [ra
 {
   HMAC_CTX *c = Pget(L, 1);
   unsigned char digest[EVP_MAX_MD_SIZE];
-  size_t written = 0;
+  unsigned int written = 0;
   
   if (lua_isstring(L, 2))
   {  
@@ -138,7 +138,7 @@ static int Lfdigest(lua_State *L)	/** digest(type, s, 
 {
   HMAC_CTX c;
   unsigned char digest[EVP_MAX_MD_SIZE];
-  size_t written = 0;
+  unsigned int written = 0;
   const char *t = luaL_checkstring(L, 1);
   const char *s = luaL_checkstring(L, 2);
   const char *k = luaL_checkstring(L, 3);
