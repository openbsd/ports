--- src/Makefile.in.orig	Thu Mar 16 00:03:39 2000
+++ src/Makefile.in	Tue Mar 28 23:54:04 2000
@@ -50,12 +50,12 @@
 
 install: all
 	for prog in $(PROGS) $(SCRIPTS) ; do \
-		$(INSTALL_PROGRAM) $$prog $(bindir) ; \
+		$(INSTALL) $$prog $(DESTDIR)$(bindir) ; \
 	done
-	-strip $(bindir)/remind
-	-strip $(bindir)/rem2ps
+	strip $(bindir)/remind
+	strip $(bindir)/rem2ps
 	for man in $(MANS) ; do  \
-		$(INSTALL_DATA) $$man $(mandir)/man1 ; \
+		$(INSTALL_DATA) $$man $(DESTDIR)$(mandir)/man1 ; \
 	done
 
 clean:
