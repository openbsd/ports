Index: doc/Makefile
--- doc/Makefile.orig
+++ doc/Makefile
@@ -185,7 +185,7 @@ INSTALL_SCRIPT = ${INSTALL}
 INSTALL_STRIP_PROGRAM = $(install_sh) -c -s
 LDFLAGS = 
 LIBOBJS = 
-LIBS = -levent 
+LIBS = -levent_core
 LTLIBOBJS = 
 MAKEINFO = ${SHELL} '/home/dormando/p/code/memcached/missing' makeinfo
 MKDIR_P = /usr/bin/mkdir -p
