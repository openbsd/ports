Index: tests/CMakeLists.txt
--- tests/CMakeLists.txt.orig
+++ tests/CMakeLists.txt
@@ -68,6 +68,7 @@ function(TEST NAME FILE)
 		subsurface_backend_shared
 		${TEST_SPECIFIC_LIBRARIES}
 		subsurface_corelib
+		subsurface_dc
 		RESOURCE_LIBRARY
 		${QT_TEST_LIBRARIES}
 		${SUBSURFACE_LINK_LIBRARIES}
@@ -103,6 +104,7 @@ add_executable(TestQML testqml.cpp)
 target_link_libraries(
 	TestQML
 	subsurface_corelib
+	subsurface_dc
 	RESOURCE_LIBRARY
 	${QT_TEST_LIBRARIES}
 	${SUBSURFACE_LINK_LIBRARIES}
