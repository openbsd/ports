Index: configure
--- configure.orig
+++ configure
@@ -16251,7 +16251,7 @@ if ${mc_cv_slang_termcap+:} false; then :
 else
 
 	ac_save_LIBS="$LIBS"
-	LIBS="$LIBS -lslang"
+	LIBS="$LIBS -lslang -ltermcap"
 	cat confdefs.h - <<_ACEOF >conftest.$ac_ext
 /* end confdefs.h.  */
 
@@ -23277,7 +23277,7 @@ if test -n "$EXT2FS_CFLAGS"; then
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_EXT2FS_CFLAGS=`$PKG_CONFIG --cflags "ext2fs >= 1.42.4" 2>/dev/null`
+  pkg_cv_EXT2FS_CFLAGS=`$PKG_CONFIG --cflags "ext2fs <= 1.42.4" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -23527,7 +23527,7 @@ done
   done
 IFS=$as_save_IFS
 
-  test -z "$ac_cv_path_ZIP" && ac_cv_path_ZIP="/usr/bin/zip"
+  ac_cv_path_ZIP="${LOCALBASE}/bin/zip"
   ;;
 esac
 fi
@@ -23568,7 +23568,7 @@ done
   done
 IFS=$as_save_IFS
 
-  test -z "$ac_cv_path_UNZIP" && ac_cv_path_UNZIP="/usr/bin/unzip"
+  ac_cv_path_UNZIP="${LOCALBASE}/bin/unzip"
   ;;
 esac
 fi
