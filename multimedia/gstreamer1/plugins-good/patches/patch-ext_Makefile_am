$OpenBSD: patch-ext_Makefile_am,v 1.3 2012/09/18 07:54:14 ajacoutot Exp $
--- ext/Makefile.am.orig	Sun Sep 16 22:38:16 2012
+++ ext/Makefile.am	Tue Sep 18 08:59:53 2012
@@ -77,6 +77,12 @@ else
 SHOUT2_DIR =
 endif
 
+if USE_SNDIO
+SNDIO_DIR = sndio
+else
+SNDIO_DIR =
+endif
+
 if USE_SOUP
 SOUP_DIR=soup
 else
@@ -122,6 +128,7 @@ SUBDIRS = \
 	$(MIKMOD_DIR) \
 	$(PULSE_DIR) \
 	$(SHOUT2_DIR) \
+	$(SNDIO_DIR) \
 	$(SOUP_DIR) \
 	$(SPEEX_DIR) \
 	$(TAGLIB_DIR) \
@@ -141,6 +148,7 @@ DIST_SUBDIRS = \
 	pulse \
 	raw1394 \
 	shout2 \
+	sndio \
 	soup \
 	speex \
 	taglib \
