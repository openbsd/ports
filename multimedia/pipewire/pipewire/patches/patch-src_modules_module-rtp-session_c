No SIOCGIFINDEX on OpenBSD; use POSIX if_nametoindex(3) and store the
index where the multicast join code expects it.

Index: src/modules/module-rtp-session.c
--- src/modules/module-rtp-session.c.orig
+++ src/modules/module-rtp-session.c
@@ -1097,9 +1097,9 @@ static int make_socket(const struct sockaddr_storage* 
 	spa_zero(req);
 	if (ifname) {
 		snprintf(req.ifr_name, sizeof(req.ifr_name), "%s", ifname);
-		res = ioctl(fd, SIOCGIFINDEX, &req);
+		res = (req.ifr_ifindex = if_nametoindex(ifname)) == 0 ? -1 : 0;
 	        if (res < 0)
-	                pw_log_warn("SIOCGIFINDEX %s failed: %m", ifname);
+	                pw_log_warn("if_nametoindex %s failed: %m", ifname);
 	}
 	res = 0;
 	if (af == AF_INET) {
