$OpenBSD: patch-ext_Makefile_am,v 1.4 2010/07/22 19:17:33 ajacoutot Exp $
--- ext/Makefile.am.orig	Thu Jun  3 20:54:01 2010
+++ ext/Makefile.am	Wed Jul 21 19:51:24 2010
@@ -80,6 +80,12 @@ else
 LIBPNG_DIR =
 endif
 
+if USE_LIBSNDIO
+LIBSNDIO_DIR = libsndio
+else
+LIBSNDIO_DIR =
+endif
+
 # if USE_MIKMOD
 # MIKMOD_DIR = mikmod
 # else
@@ -144,6 +150,7 @@ SUBDIRS = \
 	$(LIBDV_DIR) \
 	$(LIBMNG_DIR) \
 	$(LIBPNG_DIR) \
+	$(LIBSNDIO_DIR) \
 	$(MIKMOD_DIR) \
 	$(PULSE_DIR) \
 	$(SHOUT2_DIR) \
@@ -165,6 +172,7 @@ DIST_SUBDIRS = \
 	jpeg \
 	libcaca \
 	libpng \
+	libsndio \
 	pulse \
 	raw1394 \
 	shout2 \
