COMMENT-main =	monospaced OpenType fonts designed for coding
COMMENT-web =	monospaced OpenType fonts designed for coding (web fonts)

ADOBE_PROJECT =		source-code-pro
ADOBE_TAGNAME =		${VERSION}R-u/${VERSION_I}R-i/${VERSION_V}R-vf

MULTI_PACKAGES =	-main -web

MODFONT_VERSION =	${VERSION}

VERSION =	2.042
VERSION_I =	1.062
VERSION_V =	1.026

DISTFILES =	OTF-source-code-pro-${VERSION}R-u_${VERSION_I}R-i.zip \
		TTF-source-code-pro-${VERSION}R-u_${VERSION_I}R-i.zip \
		VF-source-code-VF-${VERSION_V}R.zip \
		WOFF2-source-code-pro-${VERSION}R-u_${VERSION_I}R-i_${VERSION_V}Rvf.zip

MODFONT_FONTFILES =	OTF/*.otf \
			TTF/*.ttf \
			VF/*.otf
MODFONT_WEBFILES =	WOFF2/*/*.woff2

.include <bsd.port.mk>
