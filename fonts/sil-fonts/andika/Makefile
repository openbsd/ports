COMMENT-main =		clear sans serif, Unicode-compliant font
COMMENT-web =		clear sans serif, Unicode-compliant font (web fonts)

MULTI_PACKAGES =	-main -web

MODFONT_FAMILY =	andika
MODFONT_VERSION =	7.000
DISTNAME =		Andika-${MODFONT_VERSION}

.include <bsd.port.mk>
