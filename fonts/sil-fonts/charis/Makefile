COMMENT-main =		readable Unicode font for laser printers
COMMENT-web =		readable Unicode font for laser printers (web fonts)

MULTI_PACKAGES =	-main -web

MODFONT_FAMILY =	charis
MODFONT_VERSION =	7.000
DISTNAME =		Charis-${MODFONT_VERSION}

.include <bsd.port.mk>
