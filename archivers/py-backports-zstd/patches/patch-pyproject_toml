Index: pyproject.toml
--- pyproject.toml.orig
+++ pyproject.toml
@@ -31,7 +31,7 @@ Source = "https://github.com/rogdham/backports.zstd"
 #
 
 [build-system]
-requires = ["setuptools>=80"]
+requires = ["setuptools"]
 build-backend = "setuptools.build_meta"
 
 [tool.setuptools.packages.find]
