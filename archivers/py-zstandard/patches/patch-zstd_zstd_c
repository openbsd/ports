we don't have qsort_r

Index: zstd/zstd.c
--- zstd/zstd.c.orig
+++ zstd/zstd.c
@@ -87,7 +87,7 @@
  */
 #if defined(__linux) || defined(__linux__) || defined(linux) || defined(__gnu_linux__) || \
     defined(__CYGWIN__) || defined(__MSYS__)
-#if !defined(_GNU_SOURCE) && !defined(__ANDROID__) /* NDK doesn't ship qsort_r(). */
+#if !defined(_GNU_SOURCE) && !defined(__ANDROID__) && !defined(__OpenBSD__) /* NDK doesn't ship qsort_r(). */
 #define _GNU_SOURCE
 #endif
 #endif
