Fix CVE-2024-26256 (#2269)
https://github.com/libarchive/libarchive/commit/b7b0c7c437

Index: libarchive/archive_read_support_format_rar.c
--- libarchive/archive_read_support_format_rar.c.orig
+++ libarchive/archive_read_support_format_rar.c
@@ -3389,6 +3389,12 @@ run_filters(struct archive_read *a)
       return 0;
   }
 
+  if (filter->blocklength > VM_MEMORY_SIZE)
+  {
+    archive_set_error(&a->archive, ARCHIVE_ERRNO_FILE_FORMAT, "Bad RAR file data");
+    return 0;
+  }
+
   ret = copy_from_lzss_window(a, filters->vm->memory, start, filter->blocklength);
   if (ret != ARCHIVE_OK)
     return 0;
