COMMENT =		Python library for "Tom's Obvious Minimal Language" #'

MODPY_DISTV =	0.10.2
DISTNAME =		toml-${MODPY_DISTV}
PKGNAME =		py-${DISTNAME}
CATEGORIES =		textproc
REVISION =		4

# MIT
PERMIT_PACKAGE=	Yes

MODULES=		lang/python

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools

.include <bsd.port.mk>
