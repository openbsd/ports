COMMENT=	sphinx extension to render display math in HTML
VERSION=	1.0.1
REVISION=	5
DISTNAME=	sphinxcontrib-${CONTRIB}-${MODPY_DISTV}

MODULES=	lang/python
MODPY_PYBUILD=	setuptools

.include <bsd.port.mk>
