COMMENT=	define simple search patterns in bulk to perform

MODPY_DISTV=	6.0.1
DISTNAME=	rebulk-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}

CATEGORIES=	textproc

HOMEPAGE=	https://github.com/Toilal/rebulk/

# MIT
PERMIT_PACKAGE=	Yes

MODULES=	lang/python
MODPY_PYBUILD=	uv_build
MODPY_PI=	Yes

.include <bsd.port.mk>
