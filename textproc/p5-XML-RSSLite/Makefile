COMMENT=	lightweight, 'relaxed' RSS (and XML-ish) parser

MODULES=	cpan
PKG_ARCH=	*
DISTNAME =	XML-RSSLite-0.17
CATEGORIES=	textproc

# Perl
PERMIT_PACKAGE=	Yes

EXTRACT_SUFX = 		.tgz

.include <bsd.port.mk>
