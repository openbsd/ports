COMMENT=	python helper for Semantic Versioning

MODPY_DISTV=	3.0.2
DISTNAME=	semver-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}
REVISION=	1

CATEGORIES=	textproc

HOMEPAGE=	https://github.com/k-bx/python-semver

# BSD3
PERMIT_PACKAGE=	Yes

MODULES=	lang/python
MODPY_PYBUILD=	setuptools_scm
MODPY_PI=	Yes

TEST_DEPENDS=	devel/py-test-cov

.include <bsd.port.mk>
