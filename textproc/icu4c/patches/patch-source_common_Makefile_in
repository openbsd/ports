$OpenBSD: patch-source_common_Makefile_in,v 1.11 2018/12/09 21:00:26 ajacoutot Exp $

Don't strip static libraries.

Index: source/common/Makefile.in
--- source/common/Makefile.in.orig
+++ source/common/Makefile.in
@@ -153,7 +153,7 @@ install-local: install-headers install-library
 install-library: all-local
 	$(MKINSTALLDIRS) $(DESTDIR)$(libdir)
 ifneq ($(ENABLE_STATIC),)
-	$(INSTALL-L) $(TARGET) $(DESTDIR)$(libdir)
+	$(INSTALL_DATA) $(TARGET) $(DESTDIR)$(libdir)
 endif
 ifneq ($(ENABLE_SHARED),)
 	$(INSTALL-L) $(FINAL_SO_TARGET) $(DESTDIR)$(libdir)
