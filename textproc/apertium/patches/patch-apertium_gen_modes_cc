Index: apertium/gen_modes.cc
--- apertium/gen_modes.cc.orig
+++ apertium/gen_modes.cc
@@ -30,6 +30,7 @@
 #include <map>
 #include <string>
 #include <set>
+#include <unistd.h>
 
 using namespace std;
 
@@ -306,6 +307,11 @@ void gen_modes(map<string, pipeline>& modes, fs::path&
 
 int main(int argc, char* argv[])
 {
+  if (pledge("stdio rpath wpath cpath", NULL) == -1) {
+    cerr << "pledge" << endl;
+    exit(EXIT_FAILURE);
+  }
+
   LtLocale::tryToSetLocale();
 
 #if HAVE_GETOPT_LONG
