COMMENT=		PEG Parser-Interpreter in Python

MODPY_DISTV=	2.15.2
DISTNAME=		pyPEG2-${MODPY_DISTV}
PKGNAME=		${DISTNAME:S/^py/${MODPY_PY_PREFIX}/}
REVISION=		8

CATEGORIES=		textproc

# GPLv2
PERMIT_PACKAGE=	Yes

MODULES=		lang/python
MODPY_PI=		Yes
MODPY_PYBUILD=		setuptools

.include <bsd.port.mk>
