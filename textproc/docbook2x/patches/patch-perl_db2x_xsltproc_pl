$OpenBSD: patch-perl_db2x_xsltproc_pl,v 1.1 2021/09/02 18:26:14 ajacoutot Exp $

Index: perl/db2x_xsltproc.pl
--- perl/db2x_xsltproc.pl.orig
+++ perl/db2x_xsltproc.pl
@@ -110,10 +110,10 @@ sub check_options
     
     if($options->{'stylesheet'} eq 'texi') {
         $options->{'stylesheet'} = 
-            "http://docbook2x.sf.net/latest/xslt/texi/docbook.xsl";
+            "${PREFIX}/share/docbook2X/xslt/texi/docbook.xsl";
     } elsif($options->{'stylesheet'} eq 'man') {
         $options->{'stylesheet'} = 
-            "http://docbook2x.sf.net/latest/xslt/man/docbook.xsl";
+            "${PREFIX}/share/docbook2X/xslt/man/docbook.xsl";
     }
 
     if(scalar(@argv) != 1) {
