Index: deformatters/Makefile
--- deformatters/Makefile.orig
+++ deformatters/Makefile
@@ -80,12 +80,12 @@ all:	$(PROGRAMS)
 defmt-c: defmt-c.o
 	@. ../config.sh; \
 	  set -x; \
-	  $$CC $$CFLAGS -o $@ $<
+	  $$CC $$CFLAGS -o $@ $>
 
 defmt-sh: defmt-sh.o
 	@. ../config.sh; \
 	  set -x; \
-	  $$CC $$CFLAGS -o $@ $<
+	  $$CC $$CFLAGS -o $@ $>
 
 .c.o:
 	@. ../config.sh; \
