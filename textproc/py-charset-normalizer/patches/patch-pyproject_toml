Index: pyproject.toml
--- pyproject.toml.orig
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools", "setuptools-scm", "mypy>=1.4.1,<=1.14.0"]
+requires = ["setuptools", "setuptools-scm", "mypy>=1.4.1"]
 build-backend = "setuptools.build_meta"
 
 [project]
