COMMENT =		markdown parser for docutils

MODPY_DISTV =	0.7.1
# Using github autogenerated tarball because pypi tarballs don't ship
# files needed by tests.
GH_ACCOUNT =		readthedocs
GH_PROJECT =		recommonmark
GH_TAGNAME =		${MODPY_DISTV}
PKGNAME =		py-recommonmark-${MODPY_DISTV}
REVISION =		5

CATEGORIES =		textproc devel

# MIT
PERMIT_PACKAGE =	Yes

MODULES =		lang/python

MODPY_PYBUILD =		setuptools

RUN_DEPENDS =		textproc/py-commonmark>=0.8.1 \
			textproc/py-docutils \
			textproc/py-sphinx

.include <bsd.port.mk>
