COMMENT=	view for Pod::POM that outputs reStructuredText

MODULES=	cpan
PKG_ARCH=	*
DISTNAME=	Pod-POM-View-Restructured-1.000003
CATEGORIES=	textproc

# perl
PERMIT_PACKAGE=	Yes

RUN_DEPENDS=		textproc/p5-Pod-POM

.include <bsd.port.mk>
