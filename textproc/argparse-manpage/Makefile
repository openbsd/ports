COMMENT=		build manual page from python's ArgumentParser object

MODPY_DISTV=	4.5
DISTNAME=		argparse-manpage-${MODPY_DISTV}
REVISION=		1

CATEGORIES=		textproc

# Apache-2.0
PERMIT_PACKAGE=		Yes

SITES=			https://github.com/praiskup/argparse-manpage/releases/download/v${MODPY_DISTV}/

MODULES=		lang/python

MODPY_PYBUILD=		setuptools

TEST_DEPENDS=		devel/py-pip

.include <bsd.port.mk>
