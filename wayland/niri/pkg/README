+-------------------------------------------------------------------------------
| Running ${PKGSTEM} on OpenBSD
+-------------------------------------------------------------------------------

On OpenBSD, use the provided ${PREFIX}/bin/niri-session script to
launch niri from an text VT (xenodm must be stopped).
