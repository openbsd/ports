Uses SOCK_CLOEXEC, SOCK_NONBLOCK

Index: meson.build
--- meson.build.orig
+++ meson.build
@@ -72,7 +72,7 @@ c_args = [
   '-g',
   '-Wno-unused-function',
   '-DWLR_USE_UNSTABLE',
-  '-D_POSIX_C_SOURCE=200809L',
+  '-D_POSIX_C_SOURCE=202405L',
   '-DVERSION="@0@"'.format(version_with_hash),
   '-DSYSCONFDIR="@0@"'.format('/etc'),
 ]
