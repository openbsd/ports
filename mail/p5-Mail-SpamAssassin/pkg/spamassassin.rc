#!/bin/sh
#
# $OpenBSD: spamassassin.rc,v 1.5 2011/12/17 22:01:13 sthen Exp $

daemon="${TRUEPREFIX}/bin/spamd -d"
daemon_flags="-u _spamdaemon -P"

. /etc/rc.d/rc.subr

pexp="perl: ${daemon}${daemon_flags:+ ${daemon_flags}}"

rc_cmd $1
