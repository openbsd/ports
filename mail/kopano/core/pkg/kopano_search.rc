#!/bin/sh
#
# $OpenBSD: kopano_search.rc,v 1.2 2017/11/25 10:59:19 robert Exp $

daemon="${TRUEPREFIX}/sbin/kopano-search"

. /etc/rc.d/rc.subr

pexp="${MODPY_BIN} ${daemon}${daemon_flags:+ ${daemon_flags}}"

rc_pre() {
	install -d -o _kopano -g _kopano /var/run/kopano
}

rc_cmd $1
