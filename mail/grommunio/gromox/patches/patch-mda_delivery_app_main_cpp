Index: mda/delivery_app/main.cpp
--- mda/delivery_app/main.cpp.orig
+++ mda/delivery_app/main.cpp
@@ -167,7 +167,7 @@ int main(int argc, const char **argv) try
 		return EXIT_FAILURE;
     }
 	auto cleanup_6 = make_scope_exit(system_services_stop);
-	auto dummy_sk = gx_local_listen("/run/gromox/da-runcheck");
+	auto dummy_sk = gx_local_listen("/var/run/gromox/da-runcheck");
 	if (dummy_sk < 0) {
 		mlog(LV_ERR, "gromox-delivery is already running");
 		return EXIT_FAILURE;
