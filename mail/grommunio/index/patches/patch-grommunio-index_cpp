Index: grommunio-index.cpp
--- grommunio-index.cpp.orig
+++ grommunio-index.cpp
@@ -1242,11 +1242,11 @@ static void load_index_config()
 			dest = it->second;
 	};
 	if (index_root.empty())
-		index_root = "/var/lib/grommunio-web/sqlite-index";
+		index_root = "/var/www/cache/sqlite-index";
 	if (switch_user.empty())
-		switch_user = "groindex";
+		switch_user = "www";
 	if (switch_group.empty())
-		switch_group = "groweb";
+		switch_group = "daemon";
 	apply("index_root", index_root);
 	apply("user", switch_user);
 	apply("group", switch_group);
