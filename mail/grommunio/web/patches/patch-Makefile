Index: Makefile
--- Makefile.orig
+++ Makefile
@@ -5,7 +5,7 @@
 
 # Tools
 
-SHELL := /bin/bash
+SHELL := /usr/local/bin/bash
 
 PHPMD ?= phpmd
 PHPDOC ?= phpdoc
@@ -17,7 +17,9 @@ PHP ?= php
 
 # Variables
 
-DESTDIR ?= deploy
+ifeq ($(DESTDIR),)
+DESTDIR := deploy
+endif
 
 # Javascript compiler
 
@@ -143,7 +145,7 @@ $(eval $(call vendor_tree,filepreviewer,ViewerJS/webod
 js: $(VENDORED) $(JSDEPLOY)/fingerprint.js $(JSDEPLOY)/resize.js $(JSDEPLOY)/grommunio.js $(JSDEPLOY)/extjs-mod/extjs-mod.js $(JSDEPLOY)/extjs/ext-base-all.js $(JSDEPLOY)/extjs/ext-base-all-debug.js $(DESTDIR)/client/third-party/ux-thirdparty.js $(DEPLOYPURIFYJS) $(addprefix $(JSDEPLOY)/filepreviewer/,$(VIEWERJS))
 
 $(DESTDIR)/%.php: %.php
-	${PHP} -l $<
+	#${PHP} -l $<
 	mkdir -p $$(dirname $@)
 	cp $< $@
 
