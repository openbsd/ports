$OpenBSD: patch-index_c,v 1.1 2020/01/08 00:10:30 sthen Exp $

From 468774858c6b9f93e28b0f3696147f39c7933762 Mon Sep 17 00:00:00 2001
From: Pietro Cerutti <gahr@gahr.ch>
Date: Wed, 11 Dec 2019 10:15:49 -0500
Subject: [PATCH] Fix uncollapse_jump (#2037)

Fixes https://github.com/neomutt/neomutt/issues/2036

Index: index.c
--- index.c.orig
+++ index.c
@@ -2504,10 +2504,10 @@ int mutt_index_menu(void)
           mutt_check_traditional_pgp(&el, &menu->redraw);
           emaillist_clear(&el);
         }
-        struct Email *e = get_cur_email(Context, menu);
-        if (!e)
+        e_cur = get_cur_email(Context, menu);
+        if (!e_cur)
           break;
-        int hint = e->index;
+        int hint = e_cur->index;
 
         op = mutt_display_message(MuttIndexWindow, Context->mailbox, e_cur);
         if (op < 0)
