Make bootstrap paths for data directory adhere to OpenBSD folder structure

Index: crates/jmap/src/registry/mapping/bootstrap.rs
--- crates/jmap/src/registry/mapping/bootstrap.rs.orig
+++ crates/jmap/src/registry/mapping/bootstrap.rs
@@ -653,7 +653,7 @@ fn map_dns_server(dns_server: &DnsServerBootstrap) -> 
 
 // FreeBSD keeps variable application data under /var/db (hier(7))
 // rather than FHS /var/lib.
-const DEFAULT_DATA_PATH: &str = if cfg!(target_os = "freebsd") {
+const DEFAULT_DATA_PATH: &str = if cfg!(target_os = "freebsd") || cfg!(target_os = "openbsd") {
     "/var/db/stalwart/"
 } else {
     "/var/lib/stalwart/"
