*** filters/ikon2icon.c.orig	Thu Dec  4 20:18:34 1997
--- filters/ikon2icon.c	Thu Dec  4 20:18:35 1997
***************
*** 18,24 ****
  
  #define  PATCHLEVEL  1
  
! #define  GETS        (void) gets     /* To make lint happy. */
  #define  PRINTF      (void) printf
  #define  SPRINTF     (void) sprintf
  #define  SSCANF      (void) sscanf
--- 18,24 ----
  
  #define  PATCHLEVEL  1
  
! #define  GETS        (void) fgets     /* To make lint happy. */
  #define  PRINTF      (void) printf
  #define  SPRINTF     (void) sprintf
  #define  SSCANF      (void) sscanf
***************
*** 65,71 ****
  
    for (i = 0; i < BLITHEIGHT; i++)
      {
!       GETS(nextline) ;
        ptr = nextline ;
        for (j = 0; j < 3; j++)
          {
--- 65,71 ----
  
    for (i = 0; i < BLITHEIGHT; i++)
      {
!       GETS(nextline, MAXLINE - 1, stdin) ;
        ptr = nextline ;
        for (j = 0; j < 3; j++)
          {
