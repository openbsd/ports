Index: src/scan.c
--- src/scan.c.orig
+++ src/scan.c
@@ -77,7 +77,7 @@ void scan_mailbox (char path[])
      /* {{{  */
 {
   static FILE *outf;
-  extern FILE *tmpp;
+  extern void *tmpp;
   static mbox_t *mbox, *out;
 #ifdef HAVE_LIBPCRE
   extern pcre *pcre_pattern;
