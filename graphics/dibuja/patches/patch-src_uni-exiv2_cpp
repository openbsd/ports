Fix build with exiv2 >=0.28.0.

Index: src/uni-exiv2.cpp
--- src/uni-exiv2.cpp.orig
+++ src/uni-exiv2.cpp
@@ -27,7 +27,7 @@
 
 #include "uni-exiv2.hpp"
 
-static Exiv2::Image::AutoPtr cached_image;
+static Exiv2::Image::UniquePtr cached_image;
 #if 0
 extern "C"
 void 
@@ -51,7 +51,7 @@ uni_read_exiv2_map(const char *uri, Exif2Callback call
 
     Exiv2::LogMsg::setLevel(Exiv2::LogMsg::mute);
     try {
-        Exiv2::Image::AutoPtr image = Exiv2::ImageFactory::open(uri);
+        Exiv2::Image::UniquePtr image = Exiv2::ImageFactory::open(uri);
         if ( image.get() == 0 ) {
             return;
         }
@@ -99,7 +99,7 @@ uni_read_exiv2_map(const char *uri, Exif2Callback call
                 }
             }
         }
-    } catch (Exiv2::AnyError& e) {
+    } catch (Exiv2::Error& e) {
         std::cerr << "Exiv2: '" << e << "'\n";
     }
 }
@@ -122,7 +122,7 @@ uni_read_exiv2_to_cache(const char *uri)
         }
 
         cached_image->readMetadata();
-    } catch (Exiv2::AnyError& e) {
+    } catch (Exiv2::Error& e) {
         std::cerr << "Exiv2: '" << e << "'\n";
     }
 
@@ -140,7 +140,7 @@ uni_write_exiv2_from_cache(const char *uri)
     }
 
     try {
-        Exiv2::Image::AutoPtr image = Exiv2::ImageFactory::open(uri);
+        Exiv2::Image::UniquePtr image = Exiv2::ImageFactory::open(uri);
         if ( image.get() == 0 ) {
             return 2;
         }
@@ -152,7 +152,7 @@ uni_write_exiv2_from_cache(const char *uri)
         cached_image.reset(NULL);
 
         return 0;
-    } catch (Exiv2::AnyError& e) {
+    } catch (Exiv2::Error& e) {
         std::cerr << "Exiv2: '" << e << "'\n";
     }
 
