     SUBDIR =
     SUBDIR += deskew
     SUBDIR += gegl-filters
     SUBDIR += lensfun
     SUBDIR += liquid-rescale
     SUBDIR += resynthesizer
     SUBDIR += snapshot
     SUBDIR += stable

.include <bsd.port.subdir.mk>
