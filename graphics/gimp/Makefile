     SUBDIR =
     SUBDIR += deskew
     SUBDIR += gegl-filters
     SUBDIR += lensfun
     SUBDIR += liquid-rescale
     SUBDIR += resynthesizer
     SUBDIR += snapshot
     SUBDIR += stable
     SUBDIR += stroke-arrows

.include <bsd.port.subdir.mk>
