--- configure.orig	Wed Feb 14 03:00:00 1996
+++ configure	Sat Aug 24 00:06:39 1996
@@ -541,7 +541,7 @@
 
 
 
-CFLAGS=""
+# CFLAGS=""
 
 # Extract the first word of "gcc", so it can be a program name with args.
 set dummy gcc; ac_word=$2
@@ -1161,11 +1161,9 @@
 fi
 
 X_LDFLAGS="-L$x_libraries"
-if test "x$motif_libraries" != x; then
-  X_LDFLAGS="$X_LIBS -L$motif_libraries"
-fi
-
-
+#if test "x$motif_libraries" != x; then
+#  X_LDFLAGS="$X_LIBS -L$motif_libraries"
+#fi
 
 
 echo $ac_n "checking for -lXm""... $ac_c" 1>&6
@@ -1195,16 +1193,16 @@
 LIBS="$ac_save_LIBS"
 
 fi
-if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
-  echo "$ac_t""yes" 1>&6
-  LIBS="-lXm"
-else
-  echo "$ac_t""no" 1>&6
-no_motif=yes
-fi
+#if eval "test \"`echo '$ac_cv_lib_'$ac_lib_var`\" = yes"; then
+#  echo "$ac_t""yes" 1>&6
+#  LIBS="-lXm"
+#else
+#  echo "$ac_t""no" 1>&6
+#no_motif=yes
+#fi
 
 
-LIBS="$LIBS -lXt $X_PRE_LIBS"
+LIBS="\$(MOTIFLIB) -lXt $X_PRE_LIBS"
 
 ac_safe=`echo "sys/ipc.h" | tr './\055' '___'`
 echo $ac_n "checking for sys/ipc.h""... $ac_c" 1>&6
@@ -1321,7 +1319,7 @@
 fi
 
 
-SAVE_LIBS="$LIBS -lX11 $X_EXTRA_LIBS -lm"
+SAVE_LIBS="$LIBS -lXext -lX11 $X_EXTRA_LIBS -lm"
 LIBS=""
 
 ac_header_dirent=no
@@ -1921,7 +1919,7 @@
 
 LIBS="$SAVE_LIBS $LIBS"
 
-CFLAGS="-g -Wall"
+# CFLAGS="-g -Wall"
 
 
 trap '' 1 2 15
--- plug-ins/Makefile.orig	Mon Feb 12 09:53:50 1996
+++ plug-ins/Makefile	Fri Aug 23 23:40:24 1996
@@ -22,7 +22,8 @@
 #RANLIB		=	echo
 
 # remember to add the includes
-CFLAGS		=	-O -Wall $(INCLUDE)
+CFLAGS		?=	-O -Wall
+CFLAGS		+=	$(INCLUDE)
 
 # specify how depends are remade
 MAKEDEPEND	=	gcc -MM
@@ -48,18 +49,18 @@
 
 
 # If you are using gmake use the following 2 rules...
-%: %.c $(LIBGIMP)
-	$(CC) $(CFLAGS) -o $@ $< $(LIBGIMP) -lc
-
-%.o: %.c
-	$(CC) $(CFLAGS) -c $< -o $@
+# %: %.c $(LIBGIMP)
+# 	$(CC) $(CFLAGS) -o $@ $< $(LIBGIMP) -lc
+#
+# %.o: %.c
+# 	$(CC) $(CFLAGS) -c $< -o $@
 
 # If you are using make use the following 2 rules...
-# .c: $(LIBGIMP)
-#	$(CC) $(CFLAGS) -o $@ $< $(LIBGIMP)
-#
-# .c.o:
-#	$(CC) $(CFLAGS) -c $< -o $@
+.c: $(LIBGIMP)
+	$(CC) $(CFLAGS) -o $@ $< $(LIBGIMP)
+
+.c.o:
+	$(CC) $(CFLAGS) -c $< -o $@
 
 
 gamma:  gamma.c $(LIBGIMP)
