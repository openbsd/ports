Fix build with poppler-26.01.0.

Index: src/extension/internal/pdfinput/svg-builder.cpp
--- src/extension/internal/pdfinput/svg-builder.cpp.orig
+++ src/extension/internal/pdfinput/svg-builder.cpp
@@ -32,6 +32,7 @@
 #include <poppler/GfxState.h>
 #include <poppler/Page.h>
 #include <poppler/Stream.h>
+#include <poppler/goo/gmem.h>
 
 #include "color.h"
 #include "color/cms-util.h"
@@ -2093,7 +2094,7 @@ Inkscape::XML::Node *SvgBuilder::_createImage(Stream *
         } else {
             image_stream = new ImageStream(str, width, 1, 1);
         }
-        image_stream->reset();
+        image_stream->rewind();
 
         // Convert grayscale values
         unsigned char *buffer = new unsigned char[width];
@@ -2119,7 +2120,7 @@ Inkscape::XML::Node *SvgBuilder::_createImage(Stream *
         image_stream = new ImageStream(str, width,
                                        color_map->getNumPixelComps(),
                                        color_map->getBits());
-        image_stream->reset();
+        image_stream->rewind();
 
         // Convert RGB values
         unsigned int *buffer = new unsigned int[width];
