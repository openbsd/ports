Index: rtengine/jdatasrc.cc
--- rtengine/jdatasrc.cc.orig
+++ rtengine/jdatasrc.cc
@@ -248,18 +248,14 @@ my_error_exit (j_common_ptr cinfo)
 }
 
 
-#ifdef _WIN32
-#define JVERSION	"6b  27-Mar-1998"
-#define JCOPYRIGHT_SHORT	"(C) 1998, Thomas G. Lane"
+#define JVERSION	"unknown"
+#define JCOPYRIGHT_SHORT	"unknown"
 #define JMESSAGE(code,string)	string ,
 
 const char * const jpeg_std_message_table[] = {
 #include "jerror.h"
   NULL
 };
-#else
-extern const char * const jpeg_std_message_table[];
-#endif
 
 /*
  * Actual output of an error or trace message.
