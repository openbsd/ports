COMMENT=	library for working with HTML color formats

MODPY_DISTV=	25.10.0
DISTNAME=	webcolors-${MODPY_DISTV}
PKGNAME=	py-${DISTNAME}

CATEGORIES=	www

# BSD
PERMIT_PACKAGE=	Yes

MODULES=	lang/python
MODPY_PYBUILD=	pdm
MODPY_PI=	Yes

.include <bsd.port.mk>
