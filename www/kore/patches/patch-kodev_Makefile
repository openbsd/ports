$OpenBSD: patch-kodev_Makefile,v 1.2 2020/09/01 16:50:50 fcambus Exp $

Index: kodev/Makefile
--- kodev/Makefile.orig
+++ kodev/Makefile
@@ -14,12 +14,6 @@ CFLAGS+=-Wsign-compare -Iincludes -std=c99 -pedantic
 CFLAGS+=-DPREFIX='"$(PREFIX)"'
 LDFLAGS=-lcrypto
 
-ifneq ("$(NOOPT)", "")
-	CFLAGS+=-O0
-else
-	CFLAGS+=-O2
-endif
-
 ifneq ("$(MINIMAL)", "")
 	CFLAGS+=-DKODEV_MINIMAL
 	LDFLAGS=
