Total memory from uvmexp(VM_UVMEXP), process footprint (RSS) from KERN_PROC.

Index: Source/WTF/wtf/AvailableMemory.cpp
--- Source/WTF/wtf/AvailableMemory.cpp.orig
+++ Source/WTF/wtf/AvailableMemory.cpp
@@ -46,11 +46,14 @@
 #endif
 #if OS(LINUX)
 #include <fcntl.h>
-#elif OS(FREEBSD)
+#elif OS(FREEBSD) || OS(OPENBSD)
 #include <sys/sysctl.h>
 #include <sys/types.h>
 #include <sys/user.h>
+#if OS(OPENBSD)
+#include <uvm/uvmexp.h>
 #endif
+#endif
 #include <unistd.h>
 #elif OS(WINDOWS)
 #include <windows.h>
@@ -154,6 +157,19 @@ static size_t computeAvailableMemory()
     if (!sysinfo(&info))
         return info.totalram * info.mem_unit;
     return availableMemoryGuess;
+#elif OS(OPENBSD)
+    struct uvmexp uvmexp;
+    size_t length = sizeof(uvmexp);
+    static const int uvmexp_mib[] = { CTL_VM, VM_UVMEXP };
+    long pageSize = sysconf(_SC_PAGE_SIZE);
+    if (pageSize == -1)
+        return availableMemoryGuess;
+    if (sysctl(uvmexp_mib, std::size(uvmexp_mib), &uvmexp, &length, nullptr, 0) == 0)
+        return static_cast<size_t>(uvmexp.npages) * pageSize;
+    long pages = sysconf(_SC_PHYS_PAGES);
+    if (pages == -1)
+        return availableMemoryGuess;
+    return static_cast<size_t>(pages) * pageSize;
 #elif OS(UNIX) || OS(HAIKU)
     long pages = sysconf(_SC_PHYS_PAGES);
     long pageSize = sysconf(_SC_PAGE_SIZE);
@@ -182,7 +198,7 @@ size_t availableMemory()
     return availableMemory;
 }
 
-#if PLATFORM(IOS_FAMILY) || OS(LINUX) || OS(FREEBSD)
+#if PLATFORM(IOS_FAMILY) || OS(LINUX) || OS(FREEBSD) || OS(OPENBSD)
 MemoryStatus memoryStatus()
 {
 #if PLATFORM(IOS_FAMILY)
@@ -204,6 +220,16 @@ MemoryStatus memoryStatus()
     size_t memoryFootprint = 0;
     if (!sysctl(mib.data(), mib.size(), &info, &infolen, nullptr, 0))
         memoryFootprint = static_cast<size_t>(info.ki_rssize) * pageSize();
+#elif OS(OPENBSD)
+    struct kinfo_proc info;
+    size_t infolen = sizeof(info);
+
+    std::array<int, 6> mib { CTL_KERN, KERN_PROC, KERN_PROC_PID, getpid(), static_cast<int>(sizeof(info)), 1 };
+
+    // Resident set size, like the FreeBSD code above (ki_rssize).
+    size_t memoryFootprint = 0;
+    if (!sysctl(mib.data(), mib.size(), &info, &infolen, nullptr, 0))
+        memoryFootprint = static_cast<size_t>(info.p_vm_rssize) * pageSize();
 #endif
 
     double percentInUse = static_cast<double>(memoryFootprint) / static_cast<double>(availableMemory());
