$OpenBSD: patch-cc_BUILD_gn,v 1.25 2019/08/01 08:31:59 robert Exp $

Index: cc/BUILD.gn
--- cc/BUILD.gn.orig
+++ cc/BUILD.gn
@@ -566,7 +566,7 @@ cc_test_static_library("test_support") {
   if (enable_vulkan) {
     deps += [ "//gpu/vulkan/init" ]
   }
-  if (!is_android) {
+  if (!is_android && !is_openbsd) {
     data_deps = [
       "//third_party/mesa_headers",
     ]
@@ -767,9 +767,6 @@ cc_test("cc_unittests") {
     "//ui/gfx/geometry",
     "//ui/gl",
     "//ui/gl:test_support",
-  ]
-  data_deps = [
-    "//third_party/mesa_headers",
   ]
 }
 
