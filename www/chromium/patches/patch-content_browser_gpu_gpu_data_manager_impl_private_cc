$OpenBSD: patch-content_browser_gpu_gpu_data_manager_impl_private_cc,v 1.9 2016/09/01 16:28:11 robert Exp $
--- content/browser/gpu/gpu_data_manager_impl_private.cc.orig.port	Thu Sep  1 00:03:31 2016
+++ content/browser/gpu/gpu_data_manager_impl_private.cc	Thu Sep  1 11:12:22 2016
@@ -372,7 +372,7 @@ bool GpuDataManagerImplPrivate::GpuAccessAllowed(
     // strings even if all features are blacklisted. If all GPU features are
     // disabled, the GPU process will only initialize GL bindings, create a GL
     // context, and collect full GPU info.
-#if !defined(OS_LINUX)
+#if !defined(OS_LINUX) && !defined(OS_BSD)
     if (reason) {
       *reason = "All GPU features are blacklisted.";
     }
