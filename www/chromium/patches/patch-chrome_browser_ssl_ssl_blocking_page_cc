$OpenBSD: patch-chrome_browser_ssl_ssl_blocking_page_cc,v 1.1 2014/10/13 06:49:19 robert Exp $
--- chrome/browser/ssl/ssl_blocking_page.cc.orig.port	Sat Oct 11 15:54:35 2014
+++ chrome/browser/ssl/ssl_blocking_page.cc	Sat Oct 11 16:27:58 2014
@@ -260,7 +260,7 @@ void LaunchDateAndTimeSettings() {
   // settings. Weird. TODO(palmer): Do something more graceful than ignoring
   // the user's click! crbug.com/394993
   return;
-#elif defined(OS_LINUX)
+#elif defined(OS_LINUX) || defined(OS_BSD)
   struct ClockCommand {
     const char* pathname;
     const char* argument;
@@ -310,7 +310,7 @@ void LaunchDateAndTimeSettings() {
 #if !defined(OS_CHROMEOS)
   base::LaunchOptions options;
   options.wait = false;
-#if defined(OS_LINUX)
+#if defined(OS_LINUX) || defined(OS_BSD)
   options.allow_new_privs = true;
 #endif
   base::LaunchProcess(command, options, NULL);
