$OpenBSD: patch-content_public_common_content_switches_cc,v 1.1 2014/10/13 06:49:19 robert Exp $
--- content/public/common/content_switches.cc.orig.port	Mon Oct 13 08:27:54 2014
+++ content/public/common/content_switches.cc	Mon Oct 13 08:28:18 2014
@@ -920,7 +920,7 @@ const char kDisableVaapiAcceleratedVideoEncode[] =
     "disable-vaapi-accelerated-video-encode";
 #endif
 
-#if defined(OS_LINUX) && !defined(OS_CHROMEOS)
+#if (defined(OS_BSD) || defined(OS_LINUX)) && !defined(OS_CHROMEOS)
 // Allows sending text-to-speech requests to speech-dispatcher, a common
 // Linux speech service. Because it's buggy, the user must explicitly
 // enable it so that visiting a random webpage can't cause instability.
