$OpenBSD: patch-chrome_common_pref_names_cc,v 1.25 2016/07/31 13:11:44 robert Exp $
--- chrome/common/pref_names.cc.orig.port	Wed Jul 20 21:03:21 2016
+++ chrome/common/pref_names.cc	Thu Jul 21 10:25:29 2016
@@ -919,7 +919,7 @@ const char kForceYouTubeSafetyMode[] = "settings.force
 // (for supervised users).
 const char kForceSessionSync[] = "settings.history_recorded";
 
-#if defined(OS_LINUX) && !defined(OS_CHROMEOS)
+#if (defined(OS_BSD) || defined(OS_LINUX)) && !defined(OS_CHROMEOS)
 // Linux specific preference on whether we should match the system theme.
 const char kUsesSystemTheme[] = "extensions.theme.use_system";
 #endif
@@ -1361,7 +1361,7 @@ const char kDownloadDefaultDirectory[] = "download.def
 // upgrade a unsafe location to a safe location.
 const char kDownloadDirUpgraded[] = "download.directory_upgrade";
 
-#if defined(OS_WIN) || defined(OS_LINUX) || defined(OS_MACOSX)
+#if defined(OS_WIN) || defined(OS_LINUX) || defined(OS_MACOSX) || defined(OS_BSD)
 const char kOpenPdfDownloadInSystemReader[] =
     "download.open_pdf_in_system_reader";
 #endif
