$OpenBSD: patch-content_browser_renderer_host_pepper_message_filter_cc,v 1.3 2011/12/14 16:58:19 robert Exp $
--- content/browser/renderer_host/pepper_message_filter.cc.orig	Wed Dec  7 09:01:16 2011
+++ content/browser/renderer_host/pepper_message_filter.cc	Tue Dec 13 21:23:12 2011
@@ -130,10 +130,13 @@ bool NetAddressToAddressList(const PP_Flash_NetAddress
 
 }  // namespace
 
+// This assert fails on OpenBSD for an unknown reason at the moment.
+#if !defined(OS_OPENBSD)
 // Make sure the storage in |PP_Flash_NetAddress| is big enough. (Do it here
 // since the data is opaque elsewhere.)
 COMPILE_ASSERT(sizeof(reinterpret_cast<PP_Flash_NetAddress*>(0)->data) >=
                sizeof(sockaddr_storage), PP_Flash_NetAddress_data_too_small);
+#endif
 
 const PP_Flash_NetAddress kInvalidNetAddress = { 0 };
 
