$OpenBSD: patch-media_filters_ffmpeg_glue_cc,v 1.1 2010/12/13 09:51:46 espie Exp $
--- media/filters/ffmpeg_glue.cc.orig	Tue Mar 30 07:20:43 2010
+++ media/filters/ffmpeg_glue.cc	Sun Nov 28 00:11:25 2010
@@ -39,7 +39,7 @@ int WriteContext(URLContext* h, unsigned char* buf, in
   return AVERROR_IO;
 }
 
-offset_t SeekContext(URLContext* h, offset_t offset, int whence) {
+int64_t SeekContext(URLContext* h, int64_t offset, int whence) {
   media::FFmpegURLProtocol* protocol = ToProtocol(h->priv_data);
   offset_t new_offset = AVERROR_IO;
   switch (whence) {
