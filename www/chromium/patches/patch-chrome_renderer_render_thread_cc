$OpenBSD: patch-chrome_renderer_render_thread_cc,v 1.2 2011/02/08 09:43:02 espie Exp $
--- chrome/renderer/render_thread.cc.orig	Fri Jan 21 10:20:27 2011
+++ chrome/renderer/render_thread.cc	Sun Jan 23 23:27:33 2011
@@ -964,7 +964,7 @@ void RenderThread::EnsureWebKitInitialized() {
 }
 
 void RenderThread::IdleHandler() {
-#if (defined(OS_WIN) || defined(OS_LINUX)) && defined(USE_TCMALLOC)
+#if !defined(OS_MACOSX) && defined(USE_TCMALLOC)
   MallocExtension::instance()->ReleaseFreeMemory();
 #endif
 
@@ -1040,7 +1040,7 @@ void RenderThread::OnPurgeMemory() {
   while (!v8::V8::IdleNotification()) {
   }
 
-#if (defined(OS_WIN) || defined(OS_LINUX)) && defined(USE_TCMALLOC)
+#if !defined(OS_MACOSX) && defined(USE_TCMALLOC)
   // Tell tcmalloc to release any free pages it's still holding.
   MallocExtension::instance()->ReleaseFreeMemory();
 #endif
