$OpenBSD: patch-third_party_usrsctp_BUILD_gn,v 1.2 2017/10/25 13:47:07 robert Exp $
Index: third_party/usrsctp/BUILD.gn
--- third_party/usrsctp/BUILD.gn.orig
+++ third_party/usrsctp/BUILD.gn
@@ -109,9 +109,14 @@ static_library("usrsctp") {
     "-UINET6",
   ]
 
-  if (is_linux || is_android) {
+  if ((is_linux && !is_openbsd) || is_android) {
     defines += [
       "__Userspace_os_Linux",
+      "_GNU_SOURCE",
+    ]
+  } else if (is_openbsd) {
+    defines += [
+      "__Userspace_os_OpenBSD",
       "_GNU_SOURCE",
     ]
   } else if (is_mac || is_ios) {
