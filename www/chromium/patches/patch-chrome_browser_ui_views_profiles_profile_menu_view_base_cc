$OpenBSD: patch-chrome_browser_ui_views_profiles_profile_menu_view_base_cc,v 1.1 2021/03/22 18:04:54 robert Exp $

Index: chrome/browser/ui/views/profiles/profile_menu_view_base.cc
--- chrome/browser/ui/views/profiles/profile_menu_view_base.cc.orig
+++ chrome/browser/ui/views/profiles/profile_menu_view_base.cc
@@ -573,7 +573,7 @@ void ProfileMenuViewBase::SetProfileIdentityInfo(
 
   auto avatar_image_view = std::make_unique<AvatarImageView>(image_model, this);
 
-#if defined(OS_LINUX)
+#if defined(OS_LINUX) || defined(OS_BSD)
   // crbug.com/1161166: Orca does not read the accessible window title of the
   // bubble, so we duplicate it in the top-level menu item. To be revisited
   // after considering other options, including fixes on the AT side.
