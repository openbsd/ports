$OpenBSD: patch-components_crash_content_app_BUILD_gn,v 1.2 2016/09/01 16:28:11 robert Exp $
--- components/crash/content/app/BUILD.gn.orig.port	Thu Sep  1 00:03:30 2016
+++ components/crash/content/app/BUILD.gn	Thu Sep  1 11:12:22 2016
@@ -102,7 +102,7 @@ source_set("app_non_mac_win") {
     libs = [ "log" ]
   }
 
-  if (is_android || is_linux) {
+  if (is_android || (is_linux && !is_openbsd)) {
     # Want these files on both Linux and Android.
     set_sources_assignment_filter([])
     sources += [
@@ -134,7 +134,7 @@ source_set("app_non_mac_win") {
 
       #'../breakpad/breakpad.gyp:breakpad_sender',  TODO(GYP)
     ]
-  } else if (is_posix && !is_ios) {
+  } else if (is_posix && !is_ios && !is_openbsd) {
     deps += [ "//breakpad:client" ]
   }
 }
