$OpenBSD: patch-components_crash_content_app_BUILD_gn,v 1.17 2019/08/01 08:31:59 robert Exp $

Index: components/crash/content/app/BUILD.gn
--- components/crash/content/app/BUILD.gn.orig
+++ components/crash/content/app/BUILD.gn
@@ -37,7 +37,7 @@ static_library("app") {
     sources += [ "crashpad.cc" ]
   }
 
-  if (is_linux) {
+  if (is_linux && !is_openbsd) {
     sources += [
       "breakpad_linux.cc",
       "breakpad_linux.h",
@@ -69,7 +69,7 @@ static_library("app") {
     ]
   }
 
-  if (is_android || is_linux) {
+  if (is_android || (is_linux && !is_openbsd)) {
     deps += [
       "//base:base_static",
       "//components/crash/core/common",
@@ -78,7 +78,7 @@ static_library("app") {
       "//sandbox",
     ]
 
-    if (is_linux) {
+    if (is_linux && !is_openbsd) {
       deps += [ "//third_party/breakpad:client" ]
     }
   }
