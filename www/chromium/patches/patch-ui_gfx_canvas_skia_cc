$OpenBSD: patch-ui_gfx_canvas_skia_cc,v 1.2 2014/09/11 19:04:53 robert Exp $
--- ui/gfx/canvas_skia.cc.orig.port	Wed Sep 10 01:29:26 2014
+++ ui/gfx/canvas_skia.cc	Wed Sep 10 22:05:41 2014
@@ -284,7 +284,7 @@ void Canvas::DrawStringRectWithShadows(const base::str
     Range range = StripAcceleratorChars(flags, &adjusted_text);
     bool elide_text = ((flags & NO_ELLIPSIS) == 0);
 
-#if defined(OS_LINUX)
+#if defined(OS_LINUX) || defined(OS_BSD)
     // On Linux, eliding really means fading the end of the string. But only
     // for LTR text. RTL text is still elided (on the left) with "...".
     if (elide_text) {
