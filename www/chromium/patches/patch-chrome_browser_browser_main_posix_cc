$OpenBSD: patch-chrome_browser_browser_main_posix_cc,v 1.5 2011/10/27 07:59:08 robert Exp $
--- chrome/browser/browser_main_posix.cc.orig	Fri Oct 21 10:30:28 2011
+++ chrome/browser/browser_main_posix.cc	Wed Oct 26 13:37:46 2011
@@ -224,7 +224,7 @@ void BrowserMainPartsPosix::PreEarlyInitialization() {
   if (!fd_limit_string.empty()) {
     base::StringToInt(fd_limit_string, &fd_limit);
   }
-#if defined(OS_MACOSX)
+#if defined(OS_MACOSX) || defined(OS_OPENBSD)
   // We use quite a few file descriptors for our IPC, and the default limit on
   // the Mac is low (256), so bump it up if there is no explicit override.
   if (fd_limit == 0) {
