$OpenBSD: patch-chrome_browser_chrome_content_browser_client_cc,v 1.10 2014/04/17 16:27:53 robert Exp $
--- chrome/browser/chrome_content_browser_client.cc.orig.port	Wed Apr  2 21:04:38 2014
+++ chrome/browser/chrome_content_browser_client.cc	Sat Apr 12 09:40:41 2014
@@ -181,7 +181,7 @@
 #include "chrome/browser/chrome_browser_main_posix.h"
 #endif
 
-#if defined(OS_POSIX) && !defined(OS_MACOSX)
+#if defined(OS_POSIX) && !defined(OS_MACOSX) && !defined(OS_BSD)
 #include "base/debug/leak_annotations.h"
 #include "base/linux_util.h"
 #include "components/breakpad/app/breakpad_linux.h"
@@ -493,7 +493,7 @@ void FillFontFamilyMap(const PrefService* prefs,
   }
 }
 
-#if defined(OS_POSIX) && !defined(OS_MACOSX)
+#if defined(OS_POSIX) && !defined(OS_MACOSX) && !defined(OS_BSD)
 breakpad::CrashHandlerHostLinux* CreateCrashHandlerHost(
     const std::string& process_type) {
   base::FilePath dumps_path;
@@ -1449,7 +1449,7 @@ std::string ChromeContentBrowserClient::GetCanonicalEn
 
 void ChromeContentBrowserClient::AppendExtraCommandLineSwitches(
     CommandLine* command_line, int child_process_id) {
-#if defined(OS_POSIX)
+#if defined(OS_POSIX) && !defined(OS_BSD)
   if (breakpad::IsCrashReporterEnabled()) {
     std::string enable_crash_reporter;
     GoogleUpdateSettings::GetMetricsId(&enable_crash_reporter);
@@ -2587,7 +2587,7 @@ void ChromeContentBrowserClient::GetAdditionalFileSyst
   }
 }
 
-#if defined(OS_POSIX) && !defined(OS_MACOSX)
+#if defined(OS_POSIX) && !defined(OS_MACOSX) && !defined(OS_BSD)
 void ChromeContentBrowserClient::GetAdditionalMappedFilesForChildProcess(
     const CommandLine& command_line,
     int child_process_id,
