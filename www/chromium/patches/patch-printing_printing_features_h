$OpenBSD: patch-printing_printing_features_h,v 1.6 2021/09/01 16:54:40 robert Exp $

Index: printing/printing_features.h
--- printing/printing_features.h.orig
+++ printing/printing_features.h
@@ -40,7 +40,7 @@ bool ShouldPrintUsingXps(bool source_is_pdf);
 #endif  // defined(OS_WIN)
 
 #if defined(OS_WIN) || defined(OS_MAC) || defined(OS_LINUX) || \
-    defined(OS_CHROMEOS)
+    defined(OS_CHROMEOS) || defined(OS_BSD)
 COMPONENT_EXPORT(PRINTING_BASE)
 extern const base::Feature kEnableOopPrintDrivers;
 #endif  // defined(OS_WIN) || defined(OS_MAC) || defined(OS_LINUX) ||
