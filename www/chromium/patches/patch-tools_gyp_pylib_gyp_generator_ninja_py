$OpenBSD: patch-tools_gyp_pylib_gyp_generator_ninja_py,v 1.16 2015/04/27 16:38:25 robert Exp $
--- tools/gyp/pylib/gyp/generator/ninja.py.orig.port	Wed Apr 15 09:42:33 2015
+++ tools/gyp/pylib/gyp/generator/ninja.py	Wed Apr 15 09:42:44 2015
@@ -2040,7 +2040,7 @@ def GenerateOutputForConfig(target_list, target_dicts,
     master_ninja.rule(
       'alink_thin',
       description='AR $out',
-      command='rm -f $out && $ar rcsT $arflags $out $in')
+      command='rm -f $out && $ar rcs $arflags $out $in')
 
     # This allows targets that only need to depend on $lib's API to declare an
     # order-only dependency on $lib.TOC and avoid relinking such downstream
