$OpenBSD: patch-tools_gyp_pylib_gyp_generator_ninja_py,v 1.11 2014/09/11 19:04:53 robert Exp $
--- tools/gyp/pylib/gyp/generator/ninja.py.orig.port	Wed Sep 10 01:47:55 2014
+++ tools/gyp/pylib/gyp/generator/ninja.py	Wed Sep 10 22:05:41 2014
@@ -1919,7 +1919,7 @@ def GenerateOutputForConfig(target_list, target_dicts,
     master_ninja.rule(
       'alink_thin',
       description='AR $out',
-      command='rm -f $out && $ar rcsT $out $in')
+      command='rm -f $out && $ar rcs $out $in')
 
     # This allows targets that only need to depend on $lib's API to declare an
     # order-only dependency on $lib.TOC and avoid relinking such downstream
