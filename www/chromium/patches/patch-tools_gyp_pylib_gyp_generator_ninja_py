$OpenBSD: patch-tools_gyp_pylib_gyp_generator_ninja_py,v 1.3 2013/09/29 19:04:07 robert Exp $
--- tools/gyp/pylib/gyp/generator/ninja.py.orig.port	Wed Sep 18 10:33:03 2013
+++ tools/gyp/pylib/gyp/generator/ninja.py	Sun Sep 22 16:09:42 2013
@@ -1584,7 +1584,7 @@ def GenerateOutputForConfig(target_list, target_dicts,
     master_ninja.rule(
       'alink_thin',
       description='AR $out',
-      command='rm -f $out && $ar rcsT $out $in')
+      command='rm -f $out && $ar rcs $out $in')
 
     # This allows targets that only need to depend on $lib's API to declare an
     # order-only dependency on $lib.TOC and avoid relinking such downstream
