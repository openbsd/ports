$OpenBSD: patch-chrome_worker_worker_uitest_cc,v 1.2 2011/02/08 09:43:02 espie Exp $
--- chrome/worker/worker_uitest.cc.orig	Fri Jan 21 10:20:27 2011
+++ chrome/worker/worker_uitest.cc	Sun Jan 23 23:27:34 2011
@@ -84,7 +84,7 @@ class WorkerTest : public UILayoutTest {
     // The 1 is for the browser process.
     int number_of_processes = 1 + workers +
         (UITest::in_process_renderer() ? 0 : tabs);
-#if defined(OS_LINUX)
+#if defined(OS_LINUX) || defined(OS_OPENBSD)
     // On Linux, we also have a zygote process and a sandbox host process.
     number_of_processes += 2;
 #endif
