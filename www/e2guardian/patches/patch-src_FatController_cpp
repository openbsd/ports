Index: src/FatController.cpp
--- src/FatController.cpp.orig
+++ src/FatController.cpp
@@ -194,7 +194,7 @@ void stat_rec::reset() {
                  o.proc.http_workers,
                  bc, o.http_worker_Q.size(), o.log.log_Q->size(), cnx, cps, rqx, rqs, mfd, LC);
     } else {
-        snprintf(outbuff, 200, "%ld	%d	%d	%d	%d	%ld	%ld	%ld	%ld	%d	%d", now,
+        snprintf(outbuff, 200, "%lld	%d	%d	%d	%d	%ld	%ld	%ld	%ld	%d	%d", (long long)now,
                  o.proc.http_workers,
                  bc, o.http_worker_Q.size(), o.log.log_Q->size(), cnx, cps, rqx, rqs, mfd, LC);
     }
