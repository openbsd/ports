Ingo is an email filter rules manager. It integrates nicely with
IMP and supports the following drivers:

    * Sieve (using timsieved)
    * procmail (using VFS FTP driver)
    * IMAP client-side filtering

To complete the installation, be sure that Horde is configured and
working, then see ${INSTDIR}/docs/INSTALL
