COMMENT =		high-level cross-protocol url-grabber

MODPY_DISTV =	4.1.0
DISTNAME =		urlgrabber-${MODPY_DISTV}
PKGNAME =		py-urlgrabber-${MODPY_DISTV}
REVISION =		4

CATEGORIES =		www

# LGPLv2.1
PERMIT_PACKAGE =	Yes

MODPY_PI =		Yes

MODULES =		lang/python
MODPY_PYBUILD =		setuptools

BUILD_DEPENDS =		${RUN_DEPENDS}
RUN_DEPENDS =		net/py-curl

.include <bsd.port.mk>
