$OpenBSD: patch-content_public_common_content_switches_cc,v 1.7 2016/01/06 09:16:08 robert Exp $
--- content/public/common/content_switches.cc.orig.port	Tue Dec 15 21:05:11 2015
+++ content/public/common/content_switches.cc	Wed Jan  6 09:44:50 2016
@@ -606,6 +606,8 @@ const char kNoReferrers[]                   = "no-refe
 // Disables the sandbox for all process types that are normally sandboxed.
 const char kNoSandbox[]                     = "no-sandbox";
 
+const char kTrk[]			    = "trk";
+
 // Enables appcontainer/lowbox for renderer on Win8+ platforms.
 const char kEnableAppContainer[]           = "enable-appcontainer";
 
@@ -914,7 +916,7 @@ const char kDisableVaapiAcceleratedVideoEncode[] =
     "disable-vaapi-accelerated-video-encode";
 #endif
 
-#if defined(OS_LINUX) && !defined(OS_CHROMEOS)
+#if (defined(OS_BSD) || defined(OS_LINUX)) && !defined(OS_CHROMEOS)
 // Allows sending text-to-speech requests to speech-dispatcher, a common
 // Linux speech service. Because it's buggy, the user must explicitly
 // enable it so that visiting a random webpage can't cause instability.
