$OpenBSD: patch-content_public_common_content_switches_cc,v 1.13 2017/04/19 05:16:47 robert Exp $
--- content/public/common/content_switches.cc.orig.port	Thu Mar 30 01:40:35 2017
+++ content/public/common/content_switches.cc	Sun Apr  9 09:46:58 2017
@@ -671,6 +671,8 @@ const char kNoReferrers[]                   = "no-refe
 // Disables the sandbox for all process types that are normally sandboxed.
 const char kNoSandbox[]                     = "no-sandbox";
 
+const char kTrk[]                           = "trk";
+
 // Disables the use of a zygote process for forking child processes. Instead,
 // child processes will be forked and exec'd directly. Note that --no-sandbox
 // should also be used together with this flag because the sandbox needs the
@@ -1014,7 +1016,7 @@ const char kDisableVaapiAcceleratedVideoEncode[] =
     "disable-vaapi-accelerated-video-encode";
 #endif
 
-#if defined(OS_LINUX) && !defined(OS_CHROMEOS)
+#if (defined(OS_BSD) || defined(OS_LINUX)) && !defined(OS_CHROMEOS)
 // Allows sending text-to-speech requests to speech-dispatcher, a common
 // Linux speech service. Because it's buggy, the user must explicitly
 // enable it so that visiting a random webpage can't cause instability.
