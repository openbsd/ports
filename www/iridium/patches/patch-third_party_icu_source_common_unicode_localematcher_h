Fix build with icu4c>=73.1

Index: third_party/icu/source/common/unicode/localematcher.h
--- third_party/icu/source/common/unicode/localematcher.h.orig
+++ third_party/icu/source/common/unicode/localematcher.h
@@ -198,7 +198,7 @@ class U_COMMON_API LocaleMatcher : public UMemory { (p
          * @param src Result to move contents from.
          * @stable ICU 65
          */
-        Result(Result &&src) U_NOEXCEPT;
+        Result(Result &&src) noexcept;
 
         /**
          * Destructor.
@@ -214,7 +214,7 @@ class U_COMMON_API LocaleMatcher : public UMemory { (p
          * @param src Result to move contents from.
          * @stable ICU 65
          */
-        Result &operator=(Result &&src) U_NOEXCEPT;
+        Result &operator=(Result &&src) noexcept;
 
         /**
          * Returns the best-matching desired locale.
@@ -313,7 +313,7 @@ class U_COMMON_API LocaleMatcher : public UMemory { (p
          * @param src Builder to move contents from.
          * @stable ICU 65
          */
-        Builder(Builder &&src) U_NOEXCEPT;
+        Builder(Builder &&src) noexcept;
 
         /**
          * Destructor.
@@ -329,7 +329,7 @@ class U_COMMON_API LocaleMatcher : public UMemory { (p
          * @param src Builder to move contents from.
          * @stable ICU 65
          */
-        Builder &operator=(Builder &&src) U_NOEXCEPT;
+        Builder &operator=(Builder &&src) noexcept;
 
         /**
          * Parses an Accept-Language string
@@ -548,7 +548,7 @@ class U_COMMON_API LocaleMatcher : public UMemory { (p
      * @param src source matcher
      * @stable ICU 65
      */
-    LocaleMatcher(LocaleMatcher &&src) U_NOEXCEPT;
+    LocaleMatcher(LocaleMatcher &&src) noexcept;
 
     /**
      * Destructor.
@@ -564,7 +564,7 @@ class U_COMMON_API LocaleMatcher : public UMemory { (p
      * @return *this
      * @stable ICU 65
      */
-    LocaleMatcher &operator=(LocaleMatcher &&src) U_NOEXCEPT;
+    LocaleMatcher &operator=(LocaleMatcher &&src) noexcept;
 
     /**
      * Returns the supported locale which best matches the desired locale.
