Fix build with icu4c>=73.1

Index: third_party/icu/source/common/unicode/locid.h
--- third_party/icu/source/common/unicode/locid.h.orig
+++ third_party/icu/source/common/unicode/locid.h
@@ -294,7 +294,7 @@ class U_COMMON_API Locale : public UObject { (public)
      * @param other The Locale object being moved in.
      * @stable ICU 63
      */
-    Locale(Locale&& other) U_NOEXCEPT;
+    Locale(Locale&& other) noexcept;
 
     /**
      * Destructor
@@ -320,7 +320,7 @@ class U_COMMON_API Locale : public UObject { (public)
      * @return      *this
      * @stable ICU 63
      */
-    Locale& operator=(Locale&& other) U_NOEXCEPT;
+    Locale& operator=(Locale&& other) noexcept;
 
     /**
      * Checks if two locale keys are the same.
