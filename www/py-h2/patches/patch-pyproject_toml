Index: pyproject.toml
--- pyproject.toml.orig
+++ pyproject.toml
@@ -2,7 +2,7 @@
 # https://packaging.python.org/en/latest/specifications/pyproject-toml/
 
 [build-system]
-requires = ["setuptools>=82", "wheel>=0.46.3"]
+requires = ["setuptools", "wheel>=0.46.3"]
 build-backend = "setuptools.build_meta"
 
 [project]
