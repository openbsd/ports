COMMENT =		VCR imitation for python-requests

MODPY_DISTV =	0.9.0
DISTNAME =		betamax-${MODPY_DISTV}
PKGNAME =		py-${DISTNAME:L}
REVISION =		1

CATEGORIES =		www devel

# Apache 2.0
PERMIT_PACKAGE =	Yes

MODULES =		lang/python

MODPY_PI =		Yes
MODPY_PYBUILD =		setuptools

RUN_DEPENDS =		www/py-requests

TEST_DEPENDS =		devel/py-mock

.include <bsd.port.mk>
