$OpenBSD: patch-lib_Rose_HTML_Object_Error_pm,v 1.1.1.1 2007/04/16 20:46:42 espie Exp $
--- lib/Rose/HTML/Object/Error.pm.orig	Mon Apr 16 22:40:38 2007
+++ lib/Rose/HTML/Object/Error.pm	Mon Apr 16 22:40:49 2007
@@ -3,7 +3,7 @@ package Rose::HTML::Object::Error;
 use strict;
 
 use Carp;
-use Clone::PP();
+use Clone();
 use Scalar::Util();
 
 use Rose::Object;
@@ -61,7 +61,7 @@ sub locale { shift->parent->locale }
 sub clone
 {
   my($self) = shift;
-  my $clone = Clone::PP::clone($self);
+  my $clone = Clone::clone($self);
   $clone->parent(undef);
   return $clone;
 }
