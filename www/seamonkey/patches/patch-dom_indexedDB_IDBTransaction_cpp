$OpenBSD: patch-dom_indexedDB_IDBTransaction_cpp,v 1.1 2021/06/04 11:26:54 landry Exp $

fix build failure with nss 3.66 ?

Index: dom/indexedDB/IDBTransaction.cpp
--- dom/indexedDB/IDBTransaction.cpp.orig
+++ dom/indexedDB/IDBTransaction.cpp
@@ -221,7 +221,7 @@ IDBTransaction::Create(JSContext* aCx, IDBDatabase* aD
   MOZ_ASSERT(aMode == READ_ONLY ||
              aMode == READ_WRITE ||
              aMode == READ_WRITE_FLUSH ||
-             aMode == CLEANUP);
+             aMode == CLEANUPX);
 
   RefPtr<IDBTransaction> transaction =
     new IDBTransaction(aDatabase, aObjectStoreNames, aMode);
@@ -913,7 +913,7 @@ IDBTransaction::GetMode(ErrorResult& aRv) const
     case READ_WRITE_FLUSH:
       return IDBTransactionMode::Readwriteflush;
 
-    case CLEANUP:
+    case CLEANUPX:
       return IDBTransactionMode::Cleanup;
 
     case VERSION_CHANGE:
