$OpenBSD: patch-dom_indexedDB_IDBTransaction_h,v 1.1 2021/06/04 11:26:54 landry Exp $

fix build failure with nss 3.66 ?

Index: dom/indexedDB/IDBTransaction.h
--- dom/indexedDB/IDBTransaction.h.orig
+++ dom/indexedDB/IDBTransaction.h
@@ -59,7 +59,7 @@ class IDBTransaction final (public)
     READ_ONLY = 0,
     READ_WRITE,
     READ_WRITE_FLUSH,
-    CLEANUP,
+    CLEANUPX,
     VERSION_CHANGE,
 
     // Only needed for IPC serialization helper, should never be used in code.
@@ -194,7 +194,7 @@ class IDBTransaction final (public)
     AssertIsOnOwningThread();
     return mMode == READ_WRITE ||
            mMode == READ_WRITE_FLUSH ||
-           mMode == CLEANUP ||
+           mMode == CLEANUPX ||
            mMode == VERSION_CHANGE;
   }
 
