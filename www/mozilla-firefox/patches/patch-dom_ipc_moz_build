https://phabricator.services.mozilla.com/D157554

Index: dom/ipc/moz.build
--- dom/ipc/moz.build.orig
+++ dom/ipc/moz.build
@@ -222,6 +222,11 @@ if CONFIG["MOZ_SANDBOX"] and CONFIG["OS_ARCH"] == "WIN
         "/security/sandbox/chromium-shim",
     ]
 
+if CONFIG["MOZ_SANDBOX"] and CONFIG["OS_ARCH"] == "OpenBSD":
+    LOCAL_INCLUDES += [
+        "/xpcom/build",
+    ]
+
 if CONFIG["OS_ARCH"] != "WINNT":
     LOCAL_INCLUDES += [
         "/modules/libjar",
