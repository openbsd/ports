fix mprotect(RW) returning EACCES

https://phabricator.services.mozilla.com/D258822
Index: js/xpconnect/src/xpcpublic.h
--- js/xpconnect/src/xpcpublic.h.orig
+++ js/xpconnect/src/xpcpublic.h
@@ -856,7 +856,11 @@ struct alignas(kAutomationPageSize) ReadOnlyPage final
   // TSan is confused by write access to read-only section.
   static ReadOnlyPage sInstance;
 #else
+#ifdef XP_OPENBSD
+  static const volatile ReadOnlyPage sInstance __attribute__((section(".openbsd.mutable")));
+#else
   static const volatile ReadOnlyPage sInstance;
+#endif
 #endif
 
  private:
