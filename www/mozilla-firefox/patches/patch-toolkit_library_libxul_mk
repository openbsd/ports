$OpenBSD: patch-toolkit_library_libxul_mk,v 1.1 2014/10/17 07:26:21 landry Exp $
https://hg.mozilla.org/mozilla-central/rev/7cb633f637af
remove in 34
--- toolkit/library/libxul.mk.orig	Mon Oct 13 16:35:01 2014
+++ toolkit/library/libxul.mk	Mon Oct 13 16:35:26 2014
@@ -255,8 +255,10 @@ EXTRA_DSO_LDOPTS += $(call EXPAND_LIBNAME_PATH,StaticX
 # the linker script is given after crtbegin.o, and even past that, replaces
 # the default section rules with those from the script instead of
 # supplementing them. Which leads to a lib with a huge load of sections.
+ifneq (OpenBSD,$(OS_TARGET))
 ifdef LD_IS_BFD
 EXTRA_DSO_LDOPTS += $(topsrcdir)/toolkit/library/StaticXULComponents.ld
+endif
 endif
 
 ifeq (WINNT,$(OS_TARGET))
