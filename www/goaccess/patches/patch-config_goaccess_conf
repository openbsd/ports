Index: config/goaccess.conf
--- config/goaccess.conf.orig
+++ config/goaccess.conf
@@ -21,6 +21,16 @@
 #
 #time-format %s
 
+# httpd(8) common log format
+#date-format %d/%b/%Y
+#time-format %T %z
+#log-format %v %h %^ %^ [%d:%t] "%r" %s %b
+
+# httpd(8) combined log format
+#date-format %d/%b/%Y
+#time-format %T %z
+#log-format %v %h %^ %^ [%d:%t] "%r" %s %b "%R" "%u"
+
 ######################################
 # Date Format Options (required)
 ######################################
@@ -771,23 +781,11 @@ static-file .zst
 # DB-IP is licensed under a Creative Commons Attribution 4.0 International License.
 # https://db-ip.com/db/lite.php
 
-# Or you can download them from MaxMind
-# https://dev.maxmind.com/geoip/geoip2/geolite2/
+# Free GeoLite2 databases are available in the geolite2-{asn,city,country} packages.
+#geoip-database /var/db/GeoIP/GeoLite2-ASN.mmdb
+#geoip-database /var/db/GeoIP/GeoLite2-City.mmdb
+#geoip-database /var/db/GeoIP/GeoLite2-Country.mmdb
 
-# For GeoIP2 City database:
-# Download the GeoLite2-City.mmdb.gz
-# gunzip GeoLite2-City.mmdb.gz
-#
-# For GeoIP2 Country database:
-# Download the GeoLite2-Country.mmdb.gz
-# gunzip GeoLite2-Country.mmdb.gz
-#
-# For GeoIP2 ASN database:
-# Download the GeoLite2-ASN.mmdb.gz
-# gunzip GeoLite2-ASN.mmdb.gz
-#
-#geoip-database /usr/local/share/GeoIP/GeoLite2-City.mmdb
-#geoip-database /usr/local/share/GeoIP/GeoLite2-ASN.mmdb
 
 # Exclude requests whose client address resolves to an ASN. This option can be
 # repeated and accepts either 8075 or AS8075. It requires a GeoIP2 ASN database
