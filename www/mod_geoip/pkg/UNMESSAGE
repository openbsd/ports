To completely deinstall the mod_geoip package you
need to perform these steps as root:

        edit /var/www/conf/httpd.conf
        remove the line: LoadModule geoip_module

Do not do this if you plan on re-installing the
mod_geoip package at some future time.
